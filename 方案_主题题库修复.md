# Phase 1 主题题库修复方案 v1.0

> 依据：2026-06-22 题目审核报告（P0/P1/P2 共 11 项问题）

---

## 一、P0（阻塞发布，必须立即修复）

### P0-1：28 题魔法解析 100% 缺失

| 问题 | 当前状态 | 目标 |
|------|---------|------|
| 28 题全部标注 `**魔法解析**: ❌ 缺失` | ❌ | ✅ 每道题补上 narrativeExplanation |

**替换规则：** 所有 narrativeExplanation 使用世界观词汇，禁用：
print / 字符串 / 代码 / 输出 / 变量 / 修复 / 题目

| 禁用词 | 替换为 |
|--------|--------|
| print | 显化术 / 显现 |
| 字符串 | 真言 |
| 代码 | 源语 / 咒文 |
| 输出 | 显现 / 回响 |
| 变量 | 符文容器 / 魔力印记 |
| 修复 | 净化 / 修正 |
| 题目 | 试炼 |

**工作量：** 28 道题 × 每道 1 条 ≈ 28 条叙事解析。

### P0-2：4 道题选项格式错误

| 题号 | 问题 | 修正方案 |
|------|------|---------|
| s1_01 | 选项被 Markdown 拆为 8 行碎片 | 确保每个选项是完整字符串，`narrativeDesc` 改为纯文本描述，选项只在 `options[]` 中 |
| s1_04 | 选项 `2nd_name` 中 `2` 被 Markdown 解析为列表 | 改写选项，如 `_2nd_name` 或 `second_name`，去掉数字开头 |
| s1_20 | ``\n`` 在选项中变成真实换行 | 用 `\\n` 转义或改写选项描述，避免包含换行符 |
| s1_22 | 同 s1_01，选项被拆散 | 合并为完整字符串 |

### P0-3：s1_27 逻辑自相矛盾

| 问题 | 修正方案 |
|------|---------|
| `initialCode` 与 `correctCode` 完全相同，但题目叫"修复" | 改 `initialCode` 为不换行版本（所有内容在一行或用 `end=""` 导致不换行） |

**示例：**
```
current: initialCode = correctCode = print("第一行") ...
fixed:   initialCode = print("第一行"), print("第二行"), print("第三行")  ← 用 end="" 导致同一行
         correctCode = print("第一行")\nprint("第二行")\nprint("第三行")
```

### P0-4：s1_07 解析截断

| 问题 | 修正方案 |
|------|---------|
| `explanation` 中 `'float'>` 被截断为 `'float'` 之前 | 补全为 `type() 返回 <class 'float'>`，检查 TypeScript 转义 |

---

## 二、P1（影响沉浸感，本周修）

### P1-5：术语替换未执行

**涉及文件：** `stage1_questions.ts` 中所有 `narrativeTitle`、`narrativeDesc`、`explanation` 字段。

**替换范围：** 按 P0-1 的禁用词表扫描，替换 narrative 层所有命中位置。（`technicalDesc` 保留技术术语）

### P1-6：诺克斯语气升级

**对比标准：**

| 等级 | 当前（主题库） | 目标（备用库标准） |
|------|--------------|-----------------|
| hint | "变量名在 = 左边，值在右边" | 不变（简短即可） |
| hintRoleplay | "诺克斯用翅膀指了指：'...'" | "嘿，我当年也踩过这个坑...哈哈..." |
| hintDirect | "写两行：..." | 不变（直接即可） |

**工作量：** 约 15 题的 `hintRoleplay` 需要重写。

### P1-7：补充 commonErrors

**现状：** 主题库 0 条 `commonErrors`。

**最低要求：** 每道题至少 1 条。代码题覆盖常见语法错误，概念题覆盖常见误解。

**示例（s1_19 类型冲突）：**
```typescript
commonErrors: [
  { pattern: '"5" + 3', message: '字符串和数字不能直接相加', noxHint: '我当年也这么写过。字符串和数字是两种不同的元素，得用 int() 转化。"' }
]
```

### P1-8：bk_type_2 选项重复

| 当前 | 修正 |
|------|------|
| 42 (两次) | 其中一个改为 `"42"` 或 `'42'` |

---

## 三、P2（结构问题，后续处理）

### P2-9：难度分布

**现状：** 52 题全部 1 星。

**修正：** 标记设计难度（1-3 星），约 60% 基础 + 30% 综合 + 10% 陷阱题。需逐题评估后标记。

### P2-10：阶段 2-4 缺失

**现状：** 0 题。

**计划：** 使用 AI 生成（HackerRank/LeetCode 素材），每阶段 28 题。视当前修复进度决定是否和 P0 并行。

---

## 四、修复顺序与分批

```
P0 — 阻断修复（必须先做完，否则不可发布）
  ├── P0-1: 28 道 narrativeExplanation   ← 这个最耗时
  ├── P0-2: 4 道选项格式错误              ← 5分钟
  ├── P0-3: s1_27 初始代码 vs 正确代码矛盾  ← 5分钟
  └── P0-4: s1_07 解析截断               ← 2分钟

P1 — 体验修复（建议和 P0 同一批提交）
  ├── P1-5: narrative 层术语替换           ← 批量文本替换
  ├── P1-6: 诺克斯语气升级（15 题）         ← 逐题重写
  ├── P1-7: commonErrors 补充（28 题）     ← 至少 1 条/题
  └── P1-8: bk_type_2 选项修复            ← 2分钟

P2 — 后续优化（单独一批）
  ├── P2-9: 难度分布调整
  ├── P2-10: 阶段 2-4 数据生成
  └── 节元数据填充
```

---

## 五、待你确认

| # | 事项 | 选项 |
|---|------|------|
| 1 | P0+P1 是否一次提交 | 一次性修完再发布 / 分批修、分批验证 |
| 2 | narrativeExplanation 的写作标准 | 用我的备用库风格 / 你提供示例 / 我生成你审校 |
| 3 | P0 修完后是否需要你重新审核再进 P1 | 需要 / 不需要 |
