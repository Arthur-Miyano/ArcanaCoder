# ArcanaCoder 完整题目清单

## 语法圣殿 - 变量与类型

### 变量赋值（5试炼, 无条件开放）

## s1_01 (choice)

**叙事标题**: 初识显化术

**叙事描述**: 在 Python 魔法中，print() 是最基础的显化术。以下哪个咒文能正确在虚空中显现 "Hello, World!"？

**题目**: 以下哪行代码能正确输出 Hello, World!？

**选项**:
  0. print("Hello 
  1. World!") 
  2. print(Hello 
  3. World!) 
  4. echo "Hello 
  5. World!" 
  6. console.log("Hello 
  7. World!") 

**解析**: print() 是 Python 的内置函数，字符串需要用引号包裹。

**魔法解析**: ❌ 缺失

**诺克斯指引**:
  引导: print() 是 Python 的内置输出函数。
  朋友: 诺克斯拍了拍翅膀："记住，文字需要用引号包裹——就像把咒文封印在卷轴里。"
  明示: print("Hello, World!")

**常见反噬**:
  - "缺少引号": 字符串必须用引号包裹
  - "函数名错误": Python 的输出函数是 print()，不是 echo 或 console.log

---

## s1_02 (code_fill)

**叙事标题**: 铭刻符文

**叙事描述**: 将字符串 "Hello, World!" 赋值给变量 message，然后使用 print() 显化它。

**题目**: 将字符串赋值给变量并打印。

**初始源语**:
```python
# 将字符串 "Hello, World!" 赋值给变量 message
# 然后用 print() 打印 message
message = ""
```

**正确源语**:
```python
message = "Hello, World!"
print(message)
```

**预期回响**: `Hello, World!`

**解析**: 变量用 = 赋值，print() 直接输出变量名即可显示其值。

**魔法解析**: ❌ 缺失

**诺克斯指引**:
  引导: 变量名在 = 左边，值在右边。字符串要加引号。
  朋友: 诺克斯用翅膀指了指："变量名在左边，值在右边。print() 的时候不用加引号——直接写变量名就行。"
  明示: message = "Hello, World!"
print(message)

**常见反噬**:
  - "message = Hello, World!": 字符串没有加引号
  - "print("message")": 变量名加了引号，变成了字符串字面量

---

## s1_03 (output_predict)

**叙事标题**: 多重显化

**叙事描述**: print() 可以同时显化多个符文。预测以下法阵的输出：

**题目**: 预测 print("Hello", "World") 的输出。

**选项**:
  0. HelloWorld 
  1. Hello World 
  2. Hello 
  3. World 
  4. 报错 

**初始源语**:
```python
print("Hello", "World")
```

**预期回响**: `Hello World`

**解析**: print() 多参数时默认用空格分隔，输出 "Hello World"。

**魔法解析**: ❌ 缺失

**诺克斯指引**:
  引导: print() 的多个参数之间会用空格隔开。
  朋友: "print 可以接收多个参数，它们之间默认会有一个空格。就像同时念出两个咒文，中间有个停顿。"
  明示: print("Hello", "World") 输出 "Hello World"（中间有空格）。

**常见反噬**:
  - "认为输出是 HelloWorld": print() 多参数默认会加空格分隔

---

## s1_04 (choice)

**叙事标题**: 印记命名法则

**叙事描述**: 在 Python 中，变量名（魔法印记）必须遵循特定规则。以下哪个变量名是合法的？

**题目**: 以下哪个是合法的 Python 变量名？

**选项**:
  0. 2nd_name 
  1. my-name 
  2. my_name 
  3. class 

**解析**: 变量名不能以数字开头，不能包含连字符，不能使用 Python 关键字（class）。

**魔法解析**: ❌ 缺失

**诺克斯指引**:
  引导: 变量名只能用字母、数字和下划线，数字不能开头。
  朋友: "变量名只能用字母、数字、下划线——而且数字不能放在最前面。关键字（如 class）也不能用。"
  明示: my_name 是唯一合法的变量名。

**常见反噬**:
  - "以数字开头": 变量名不能以数字开头
  - "包含连字符": 变量名不能包含连字符 -

---

## s1_05 (code_fill)

**叙事标题**: 咒文链接

**叙事描述**: 你有两段咒文："Hello" 和 "World!"。用变量存储它们，然后用 + 拼接并打印。

**题目**: 将两个字符串变量拼接后打印。

**初始源语**:
```python
# 创建两个变量 a 和 b，分别赋值为 "Hello" 和 "World!"
# 然后用 + 拼接它们并在中间加空格，最后用 print() 输出
# 期望输出：Hello World!
a = ""
b = ""
```

**正确源语**:
```python
a = "Hello"
b = "World!"
print(a + " " + b)
```

**预期回响**: `Hello World!`

**解析**: 字符串可以用 + 拼接。"Hello" + " " + "World!" 中间加空格输出 "Hello World!"。

**魔法解析**: ❌ 缺失

**诺克斯指引**:
  引导: 用 + 拼接字符串，注意中间的空格也需要用字符串表示。
  朋友: "字符串拼接就是简单的用 + 连起来。但要注意——+ 不会自动加空格，空格也是一个字符。"
  明示: print(a + " " + b)

**常见反噬**:
  - "a + b 没有空格": 直接拼接 a+b 输出 "HelloWorld!"，缺少空格

---
### 类型系统（5试炼, s1_vars）

## s1_06 (choice)

**叙事标题**: 元素识别

**叙事描述**: 诺克斯展示了几个印记，让你辨识其中封印的元素类型。其中哪个是整数类型？

**题目**: 以下哪个值是整数（int）类型？

**选项**:
  0. "25" 
  1. 25 
  2. 25.0 
  3. True 

**解析**: 25 没有引号、没有小数点，是整数 int 类型。"25" 是字符串，25.0 是浮点数。

**魔法解析**: ❌ 缺失

**诺克斯指引**:
  引导: 没有引号、没有小数点的数字就是整数。
  朋友: "数字 25 和 2 和 5 组——没有引号，没有小数点，就是纯粹的整数。"
  明示: 25 是整数。

**常见反噬**:
  - "认为 "25" 是整数": 带引号的数字是字符串不是整数

---

## s1_07 (output_predict)

**叙事标题**: 元素鉴别术

**叙事描述**: Python 的 type() 函数可以识别元素的类型。预测以下法阵的输出：

**题目**: 预测 type(3.14) 的输出。

**选项**:
  0. "<class 'int'>" 
  1. "<class 'float'>" 
  2. "<class 'str'>" 
  3. 3.14 

**初始源语**:
```python
print(type(3.14))
```

**解析**: 3.14 是浮点数（float 类型），type() 返回 <class \

**魔法解析**: ❌ 缺失

**诺克斯指引**:
  引导: 带小数点的数字是 float 类型。
  朋友: "3.14 带小数点，Python 把它归为 float 类型。type() 就是鉴定术。"

**常见反噬**:
  - "认为 type() 直接返回值本身": type() 返回的是类型对象，不是值

---

## s1_08 (choice)

**叙事标题**: 咒文编织入门

**叙事描述**: 要将变量 name="贤者" 的值嵌入字符串中，哪种方式最简洁？

**题目**: 以下哪种方式能正确输出 "你好，贤者"？

**选项**:
  0. print("你好，name") 
  1. print(f"你好，{name}") 
  2. print("你好，{name}") 
  3. print(你好，name) 

**解析**: f-string 用 f 前缀和 {} 嵌入变量。print(f"你好，{name}") 中 {name} 会被替换为变量的值。"

**魔法解析**: ❌ 缺失

**诺克斯指引**:
  引导: f-string 需要在引号前加 f，变量放在 {} 中。
  朋友: "f-string 就像编织咒文——f 前缀告诉 Python"这个字符串要嵌入变量"，花括号 {} 就是嵌入位置。"
  明示: print(f"你好，{name}")

**常见反噬**:
  - "忘记加 f 前缀": 不加 f 时 {} 不会被替换

---

## s1_09 (code_fill)

**叙事标题**: 咒文编织实战

**叙事描述**: 已知 name = "世界"，用 f-string 输出 "你好，世界！"。

**题目**: 用 f-string 输出包含变量的字符串。

**初始源语**:
```python
# 已知 name = "世界"
# 用 f-string 输出 "你好，世界！"
# 提示：print(f"...{name}...")
name = "世界"

```

**正确源语**:
```python
name = "世界"
print(f"你好，{name}！")
```

**预期回响**: `你好，世界！`

**解析**: f"你好，{name}！" 中的 {name} 被替换为变量 name 的值 "世界"。

**魔法解析**: ❌ 缺失

**诺克斯指引**:
  引导: 在引号前加 f，变量放在 {} 中。
  朋友: "先写 f，再写引号，花括号包住变量名。三步完成咒文编织。"
  明示: print(f"你好，{name}！")

**常见反噬**:
  - "print("你好，{name}！")": 忘记加 f 前缀

---

```
s1_vars: 数据缺失
```
---

### 运算符与格式化（11试炼, s1_types）

## s1_10 (choice)

**叙事标题**: 基础算术术式

**叙事描述**: Python 支持标准的算术运算。计算 2 ** 3 的结果是什么？

**题目**: 2 ** 3 在 Python 中表示什么？

**选项**:
  0. 5 
  1. 6 
  2. 8 
  3. 23 

**解析**: 2 ** 3 表示 2 的 3 次方，即 2 × 2 × 2 = 8。

**魔法解析**: ❌ 缺失

**诺克斯指引**:
  引导: ** 是指数运算符。
  朋友: "两个星号 ** 在 Python 中代表乘方——2 ** 3 就是 2 的 3 次方，不是 2 乘以 3。"
  明示: 2 ** 3 = 8（2 的 3 次方）

**常见反噬**:
  - "认为是 2*3=6": ** 不是乘法，是指数

---

## s1_11 (output_predict)

**叙事标题**: 术式优先级

**叙事描述**: 预测以下法阵会显化的结果：

**题目**: 预测 print(2 + 3 * 4) 的输出。

**选项**:
  0. 20 
  1. 14 
  2. 24 
  3. 9 

**初始源语**:
```python
print(2 + 3 * 4)
```

**预期回响**: `14`

**解析**: 先算乘法 3 * 4 = 12，再加 2 + 12 = 14。

**魔法解析**: ❌ 缺失

**诺克斯指引**:
  引导: 先乘除后加减。
  朋友: "Python 不是傻傻地从左算到右——它知道数学规则：先乘除，后加减。和你在数学课上学的一样。"
  明示: 3 * 4 = 12，再加 2 = 14。

**常见反噬**:
  - "从左到右计算 2+3=5, 5*4=20": 乘法优先级高于加法

---

## s1_12 (code_fill)

**叙事标题**: 法阵面积术式

**叙事描述**: 编写代码计算一个长方形的面积。长 length = 10，宽 width = 5，将面积存入 area 并打印。

**题目**: 计算长方形面积 length × width。

**初始源语**:
```python
# 已知 length = 10, width = 5
# 计算长方形面积并打印
# 期望输出：50
length = 10
width = 5

```

**正确源语**:
```python
length = 10
width = 5
area = length * width
print(area)
```

**预期回响**: `50`

**解析**: area = length * width 计算面积，print(area) 输出结果。

**魔法解析**: ❌ 缺失

**诺克斯指引**:
  引导: 用 area = length * width 计算并存储结果。
  朋友: "乘法用 * 符号。长方形面积就是长乘以宽，把结果存到 area 变量里再打印。"
  明示: area = length * width
print(area)

**常见反噬**:
  - "print(length * width) 没存变量": 功能正确但不存变量，后续无法复用

---

## s1_13 (choice)

**叙事标题**: 三种除法

**叙事描述**: Python 有/、//、% 三种除法运算符。7 // 3 的结果是什么？

**题目**: 7 // 3 在 Python 中的结果是什么？

**选项**:
  0. 2.333 
  1. 2.0 
  2. 2 
  3. 1 

**解析**: // 是整数除法，7 // 3 取整数部分 2，舍弃小数。

**魔法解析**: ❌ 缺失

**诺克斯指引**:
  引导: // 是整数除法，只取整数部分。
  朋友: "// 是地板除——它只保留整数部分，把小数丢掉。7 除以 3 等于 2 余 1，// 只取 2。"
  明示: 7 // 3 = 2（整数除法，舍弃小数）

**常见反噬**:
  - "认为 7//3=2.333": // 是地板除，结果是整数（截断小数）

---

## s1_14 (code_fill)

**叙事标题**: 余数术式

**叙事描述**: 用 % 运算符计算 17 除以 5 的余数，存入 remainder 并打印。

**题目**: 计算 17 % 5 并打印结果。

**初始源语**:
```python
# 用 % 运算符计算 17 除以 5 的余数
# 将结果存入 remainder 并打印
# 期望输出：2

```

**正确源语**:
```python
remainder = 17 % 5
print(remainder)
```

**预期回响**: `2`

**解析**: 17 ÷ 5 = 3 余 2，所以 17 % 5 = 2。

**魔法解析**: ❌ 缺失

**诺克斯指引**:
  引导: % 求余数，不是求商。
  朋友: "百分号 % 在 Python 中是取余运算符——它告诉你除法之后剩下多少。17 ÷ 5 = 3 余 2，所以 17 % 5 = 2。"
  明示: remainder = 17 % 5
print(remainder)

**常见反噬**:
  - "17 / 5": / 是除法不是取模

---

## s1_15 (output_predict)

**叙事标题**: 复合术式推演

**叙事描述**: 预测这个包含多种运算符的法阵的输出：

**题目**: 预测 print(2 + 3 * 4 ** 2) 的输出。

**选项**:
  0. 80 
  1. 50 
  2. 26 
  3. 38 

**初始源语**:
```python
print(2 + 3 * 4 ** 2)
```

**预期回响**: `50`

**解析**: 先算指数 4 ** 2 = 16，再算乘法 3 * 16 = 48，最后加法 2 + 48 = 50。

**魔法解析**: ❌ 缺失

**诺克斯指引**:
  引导: 先指数再乘除后加减。
  朋友: "这里的术式有层级：** 最高贵（指数），* 次之（乘法），+ 最谦卑（加法）。"
  明示: 4 ** 2 = 16，3 * 16 = 48，2 + 48 = 50。

**常见反噬**:
  - "从左到右计算": 没有按优先级计算

---

## s1_16 (choice)

**叙事标题**: 快捷铭刻术

**叙事描述**: 诺克斯想让你理解快捷赋值。x += 5 等价于什么？

**题目**: x += 5 等价于以下哪个表达式？

**选项**:
  0. x = +5 
  1. x = x + 5 
  2. x + 5 
  3. x++ 

**解析**: x += 5 是 x = x + 5 的简写形式，先读取 x 的值加 5，再赋给 x。

**魔法解析**: ❌ 缺失

**诺克斯指引**:
  引导: x += 5 等价于 x = x + 5。
  朋友: "x += 5 是一种快捷写法——先把 x 取出来加 5，再存回去。不用写两遍 x 了。"
  明示: x += 5 等价于 x = x + 5。

**常见反噬**:
  - "认为是 x = +5": += 不是 =+

---

## s1_17 (code_fix)

**叙事标题**: 术式纠错

**叙事描述**: 这段法阵试图计算 (5 + 3) × 2，但结果不对。请修复。

**题目**: 修复运算优先级问题，使结果正确。

**初始源语**:
```python
result = 5 + 3 * 2
print(result)
```

**正确源语**:
```python
result = (5 + 3) * 2
print(result)
```

**预期回响**: `16`

**解析**: 原代码 5 + 3 * 2 = 5 + 6 = 11。加括号后 (5 + 3) * 2 = 8 * 2 = 16。

**魔法解析**: ❌ 缺失

**诺克斯指引**:
  引导: 用括号改变运算优先级。
  朋友: "Python 默认先算乘法。如果你想让加法先执行，就用括号把加法围起来——(5 + 3) * 2。"
  明示: result = (5 + 3) * 2

**常见反噬**:
  - "先加后乘没加括号": 运算符优先级导致先算乘法

---

## s1_18 (choice)

**叙事标题**: 咒文复制术

**叙事描述**: Python 中字符串可以用 * 重复。print("Ha" * 3) 的输出是什么？

**题目**: "Ha" * 3 的结果是什么？

**选项**:
  0. HaHaHa 
  1. Ha3 
  2. HaHa 
  3. 报错 

**解析**: 字符串 * 整数 n 将字符串重复 n 次。"Ha" * 3 = "HaHaHa"。

**魔法解析**: ❌ 缺失

**诺克斯指引**:
  引导: 字符串 * 数字表示重复。
  朋友: "字符串乘以数字？这在 Python 中是合法的！"Ha" * 3 就是把 "Ha" 念三遍——"HaHaHa"。"
  明示: "Ha" * 3 = "HaHaHa"

**常见反噬**:
  - "认为会报错": Python 支持字符串乘法

---

## s1_19 (code_fix)

**叙事标题**: 元素冲突修复

**叙事描述**: 这段法阵试图将咒文和数字相加导致魔力反噬。请修复。

**题目**: 修复代码使 "5" + 3 能正确相加得到 8。

**初始源语**:
```python
result = "5" + 3
print(result)
```

**正确源语**:
```python
result = int("5") + 3
print(result)
```

**预期回响**: `8`

**解析**: "5" 是字符串，不能直接加数字。用 int("5") 转为整数后计算。

**魔法解析**: ❌ 缺失

**诺克斯指引**:
  引导: 用 int() 把字符串转换成数字。
  朋友: "字符串和数字在 Python 中是两种不同的元素，不能直接混合。用 int() 这个转化术把字符串变成数字。"
  明示: int("5") + 3 = 8

**常见反噬**:
  - ""5" + 3": 字符串和数字不能直接相加

---

```
s1_types: 数据缺失
```
---

### 字符串与输出（10试炼, s1_ops）

## s1_20 (output_predict)

**叙事标题**: 特殊咒文

**叙事描述**: Python 字符串中可以包含转义字符。预测以下法阵的输出：

**题目**: 预测 print("Hello\
World") 的输出。

**选项**:
  0. Hello\\nWorld 
  1. Hello World 
  2. Hello\nWorld 
  3. 报错 

**初始源语**:
```python
print("Hello\
World")
```

**预期回响**: `Hello
World`

**解析**: \
 是换行符，表示"在这里换行"。所以 Hello 在第一行，World 在第二行。

**魔法解析**: ❌ 缺失

**诺克斯指引**:
  引导: \
 表示换行。
  朋友: "反斜杠是 Python 中的"逃逸字符"——\
 的意思是"从这里换行"，不是打印出反斜杠和 n。"
  明示: Hello 在第一行，World 在第二行。

**常见反噬**:
  - "原样输出 \\n": \\n 会被解析为换行而不是两个字面字符

---

## s1_21 (code_fill)

**叙事标题**: 分隔符定制

**叙事描述**: print() 的 sep 参数可以控制多个输出之间的分隔符。用 "---" 分隔输出 "A"、"B"、"C"。

**题目**: 使用 sep 参数自定义 print() 的分隔符。

**初始源语**:
```python
# 使用 print() 的 sep 参数
# 让输出 "A"、"B"、"C" 之间用 "---" 分隔
# 期望输出：A---B---C
print("", "", "", sep="---")
```

**正确源语**:
```python
print("A", "B", "C", sep="---")
```

**预期回响**: `A---B---C`

**解析**: print("A", "B", "C", sep="---") 用 "---" 分隔三个参数。

**魔法解析**: ❌ 缺失

**诺克斯指引**:
  引导: sep 参数控制分隔符。
  朋友: "print 的 sep 参数就像咒文之间的连接词——默认是空格，但你可以改成任何字符串。"
  明示: print("A", "B", "C", sep="---")

**常见反噬**:
  - "忘记 sep 参数": 默认空格分隔不符合要求

---

## s1_22 (choice)

**叙事标题**: 咒文结尾术

**叙事描述**: print() 默认在输出末尾加换行。怎样让两个 print 输出在同一行？

**题目**: 怎样让两个 print() 的输出在同一行？

**选项**:
  0. print("A" 
  1. end="") 和 print("B" 
  2. end="") 
  3. print("A" 
  4. "B" 
  5. same=True) 
  6. print("A") + print("B") 
  7. 无法实现 

**解析**: print("A", end="") 让第一个 print 末尾不加换行，下一个 print 的输出紧接着在同一行。

**魔法解析**: ❌ 缺失

**诺克斯指引**:
  引导: end 参数控制 print 末尾的字符。
  朋友: "print 默认会在末尾加一个换行符——就像念完咒语后换一口气。用 end="" 就可以不换气，连续念两个咒语。"
  明示: print("A", end="") 和 print("B", end="")

**常见反噬**:
  - "认为 print 不能在同一行": end 参数可以控制末尾字符

---

## s1_23 (code_fill)

**叙事标题**: 交互式显化

**叙事描述**: 使用 input() 接收用户输入的名字，然后用 f-string 输出 "你好，{名字}！"。

**题目**: 读取用户输入并输出问候语。

**初始源语**:
```python
# 使用 input() 获取用户输入（如 "贤者"）
# 然后用 f-string 输出 "你好，{名字}！"
name = input()

```

**正确源语**:
```python
name = input()
print(f"你好，{name}！")
```

**预期回响**: `你好，贤者！`

**解析**: input() 读取用户输入存入 name，f-string 嵌入 name 的值输出。"

**魔法解析**: ❌ 缺失

**诺克斯指引**:
  引导: 用变量接收 input() 的返回值。
  朋友: "input() 会把用户的输入作为字符串返回。你需要用一个变量来接住它——就像用篮子接住掉下来的果实。"
  明示: name = input()
print(f"你好，{name}！")

**常见反噬**:
  - "忘记存储 input 的结果": input() 的返回值必须用变量接收

---

## s1_24 (output_predict)

**叙事标题**: 输入陷阱

**叙事描述**: input() 返回的是字符串！预测以下代码的输出（假设输入 5）：

**题目**: 假设输入 5，预测 print(input() + 10) 的结果。

**选项**:
  0. 15 
  1. "510" 
  2. 报错 
  3. 5 

**初始源语**:
```python
# 假设输入 5
print(input() + 10)
```

**解析**: input() 返回字符串 "5"，"5" + 10 报错——Python 不允许 str + int。

**魔法解析**: ❌ 缺失

**诺克斯指引**:
  引导: input() 返回的是字符串。
  朋友: "input() 把一切输入都当作咒文（字符串）来处理。即使你输入数字 5，它也会当成 "5"——所以不能直接做数学运算。"
  明示: input() 返回字符串，不能直接加数字，会报 TypeError。

**常见反噬**:
  - "认为输出 15": 字符串和数字不能直接相加

---

## s1_25 (code_fill)

**叙事标题**: 批量录入

**叙事描述**: 读取两个整数，计算它们的和并打印。用 int() 转换 input() 的返回值。

**题目**: 读取两个整数，计算和。

**初始源语**:
```python
# 用 int(input()) 读取两个整数，计算它们的和并打印
# 例如输入 3 和 5，应输出 8
# 提示：input() 返回的是字符串，需要用 int() 转为数字
a = int(input())
b = int(input())

```

**正确源语**:
```python
a = int(input())
b = int(input())
print(a + b)
```

**解析**: int(input()) 读取输入并转为整数，a + b 计算和。

**魔法解析**: ❌ 缺失

**诺克斯指引**:
  引导: 用 int(input()) 把输入转为整数。
  朋友: "用 int() 把 input() 的结果包起来——int(input()) ——这样输入的数字就变成了真正的整数，可以做运算了。"
  明示: a = int(input())
b = int(input())
print(a + b)

**常见反噬**:
  - "a = input() 后直接 a+b": 字符串不能直接做加法

---

## s1_26 (choice)

**叙事标题**: 法阵注释

**叙事描述**: 在 Python 中，注释用来解释代码的含义。以下哪个是正确的单行注释？

**题目**: 以下哪个是 Python 的单行注释？

**选项**:
  0. // 这是注释 
  1. # 这是注释 
  2. /* 这是注释 */ 
  3. <!-- 这是注释 --> 

**解析**: Python 使用 # 表示单行注释。# 后面的内容不再执行。

**魔法解析**: ❌ 缺失

**诺克斯指引**:
  引导: Python 用 # 开头写注释。
  朋友: "在 Python 中，井号 # 后面的内容不会被当作代码执行——就像在羊皮卷上写的批注。"
  明示: Python 的单行注释用 #。

**常见反噬**:
  - "使用 //": // 是 C/Java 的注释，不是 Python 的

---

## s1_27 (code_fix)

**叙事标题**: 分行显化

**叙事描述**: 这段法阵想把三行文字分别输出，但所有内容都在一行。修复它。

**题目**: 让三行文字分别在不同行输出。

**初始源语**:
```python
print("第一行")
print("第二行")
print("第三行")
```

**正确源语**:
```python
print("第一行")
print("第二行")
print("第三行")
```

**预期回响**: `第一行
第二行
第三行`

**解析**: 这段代码本身是正确的。多个 print() 默认在每个输出后加换行。

**魔法解析**: ❌ 缺失

**诺克斯指引**:
  引导: 每个 print() 默认会换行。
  朋友: "你的代码已经是正确的——每个 print 都会自动换行。但你可以试试把它们合并成一个 print，用 \
 来换行。"
  明示: 代码本来就是正确的，每个 print() 自动换行。

---

## s1_28 (code_fill)

**叙事标题**: 综合显化术

**叙事描述**: 综合运用所学：定义一个变量 name 存储你的名字，一个变量 age 存储你的年龄，用一行 f-string 输出 "我叫{name}，今年{age}岁。"

**题目**: 用 f-string 综合输出姓名和年龄。

**初始源语**:
```python
# 定义 name = "贤者" 和 age = 18
# 用 f-string 输出 "我叫贤者，今年18岁。"
# 提示：f"我叫{name}，今年{age}岁。"
name = ""
age = 0
```

**正确源语**:
```python
name = "贤者"
age = 18
print(f"我叫{name}，今年{age}岁。")
```

**预期回响**: `我叫贤者，今年18岁。`

**解析**: f-string 中用多个 {} 嵌入不同变量。变量按顺序替换为实际值。

**魔法解析**: ❌ 缺失

**诺克斯指引**:
  引导: f-string 中可以嵌入多个变量。
  朋友: "f-string 可以同时编织多个符文——每个 {} 就是一个嵌入点。一个 f 前缀，多个花括号，就能输出完整的句子。"
  明示: print(f"我叫{name}，今年{age}岁。")

**常见反噬**:
  - "忘记 f 前缀": 不加 f 的字符串中 {} 不会被替换

---

```
s1_ops: 数据缺失
```
---

### 第 2 关：列表与循环（5试炼, 无条件开放）

```
q6_choice: 数据缺失
```
---


```
q7_fill: 数据缺失
```
---


```
q8_fix: 数据缺失
```
---


```
q9_predict: 数据缺失
```
---


```
q10_free: 数据缺失
```
---

## s1_vars

### 列表基础（5试炼, 无条件开放）

```
q6_choice: 数据缺失
```
---


```
q7_fill: 数据缺失
```
---


```
q8_fix: 数据缺失
```
---


```
q9_predict: 数据缺失
```
---


```
q10_free: 数据缺失
```
---

## s1_types

### 列表基础（5试炼, 无条件开放）

```
q6_choice: 数据缺失
```
---


```
q7_fill: 数据缺失
```
---


```
q8_fix: 数据缺失
```
---


```
q9_predict: 数据缺失
```
---


```
q10_free: 数据缺失
```
---

## s1_ops

### 列表基础（5试炼, 无条件开放）

```
q6_choice: 数据缺失
```
---


```
q7_fill: 数据缺失
```
---


```
q8_fix: 数据缺失
```
---


```
q9_predict: 数据缺失
```
---


```
q10_free: 数据缺失
```
---

## s1_strings

### 列表基础（5试炼, 无条件开放）

```
q6_choice: 数据缺失
```
---


```
q7_fill: 数据缺失
```
---


```
q8_fix: 数据缺失
```
---


```
q9_predict: 数据缺失
```
---


```
q10_free: 数据缺失
```
---

## 数据库森林 - 列表与循环

### 列表基础（5试炼, 无条件开放）

```
q6_choice: 数据缺失
```
---


```
q7_fill: 数据缺失
```
---


```
q8_fix: 数据缺失
```
---


```
q9_predict: 数据缺失
```
---


```
q10_free: 数据缺失
```
---

## s2_lists


# 备用题库

## bk_cast_1 (code_fill)

**叙事标题**: 将咒文转为数字

**叙事描述**: 有一段咒文 "99"，它实际代表一个数字。用转化术将其转为真正的数字，加 1 后显现结果。

**题目**: 将字符串 "99" 转为整数并加 1，打印结果。

**初始源语**:
```python
# 将 "99" 转为整数，加 1 后打印
print(int("") + 1)
```

**正确源语**:
```python
print(int("99") + 1)
```

**预期回响**: `100`

**解析**: int() 将字符串转为整数。字符串必须表示有效的整数。

**魔法解析**: 转化术 int() 可以将形似数字的咒文（字符串）转为真正的数字元素。这样就能进行数学运算了。

**诺克斯指引**:
  引导: int() 把咒文变成数字。
  朋友: 字符串 "99" 看起来是数字，但本质上还是咒文。用 int() 这个转化术才能把它变成真正的数字。
  明示: int("99") + 1 = 100

**常见反噬**:
  - "int("99.5")": 带小数点的字符串不能直接转 int

---

## bk_cast_2 (code_fix)

**叙事标题**: 咒文与数字的冲突

**叙事描述**: 这段源语试图将咒文 "3" 与数字 4 直接相融，但元素不兼容导致魔力反噬。修复它。

**题目**: 修复代码使输出为 7。

**初始源语**:
```python
print("3" + 4)
```

**正确源语**:
```python
print(int("3") + 4)
```

**预期回响**: `7`

**解析**: "3" 是字符串，不能直接加数字。用 int() 转换。

**魔法解析**: 咒文（字符串）和数字（整数）是两种不同的元素，不能直接融合。需要用 int() 将咒文转为数字后才能相加。

**诺克斯指引**:
  引导: int() 把咒文变成数字。
  朋友: 哈哈，我当年第一次写 Python 也踩了这个坑。"3" 是咒文，4 是数字，不能直接加。用 int("3") 转化一下就好了。
  明示: int("3") + 4 = 7

**常见反噬**:
  - ""3" + 4": 字符串和数字不能直接相加

---

## bk_div_1 (output_predict)

**叙事标题**: 两种除法法则

**叙事描述**: 源语中有两种除法：真除法 / 和截断除法 //。预测 // 的结果。

**题目**: 预测输出。

**选项**:
  0. 3.333 
  1. 3 
  2. 3.0 
  3. 1 

**初始源语**:
```python
print(10 // 3)
```

**预期回响**: `3`

**解析**: // 是地板除，整数相除取整数部分。

**魔法解析**: / 是真除法，返回小数。// 是截断除法（地板除），只取整数部分，舍弃小数。这是 Python 与大多数语言不同的特性之一。

**诺克斯指引**:
  引导: // 取整数部分。
  朋友: 哈哈，第一次见到 // 我也以为是注释（其他语言用多了）。在 Python 里 // 是做地板除，10 // 3 等于 3。
  明示: 10 // 3 = 3（整数除法，舍弃小数）

**常见反噬**:
  - "认为 // 是注释": Python 的注释是 # 不是 //

---

## bk_div_2 (code_fill)

**叙事标题**: 除法后的余数

**叙事描述**: 20 颗魔力水晶平均分给 6 个学徒，计算会剩下几颗。用取余术计算。

**题目**: 计算 20 除以 6 的余数并打印。

**初始源语**:
```python
# 计算 20 % 6 并打印

```

**正确源语**:
```python
print(20 % 6)
```

**预期回响**: `2`

**解析**: 20 / 6 = 3 余 2，所以 20 % 6 = 2。

**魔法解析**: 取余术 % 计算除法后的余数。20 ÷ 6 = 3 余 2，所以 20 % 6 = 2。常用于判断奇偶、循环周期等场景。

**诺克斯指引**:
  引导: % 是取余数。
  朋友: % 这个符号第一次看确实奇怪——它表示"除法之后剩下多少"。20 % 6 = 2，因为剩下 2 颗分不完。
  明示: 20 % 6 = 2

**常见反噬**:
  - "20 / 6 代替 %": / 是除法不是取余

---

## bk_esc_1 (output_predict)

**叙事标题**: 咒文中的引号

**叙事描述**: 在咒文中嵌入双引号需要用逃脱符。预测这段源语显现的内容。

**题目**: 预测输出。

**选项**:
  0. 他说"好" 
  1. "他说'好'" 
  2. 他说好 
  3. 报错 

**初始源语**:
```python
print("他说\\"好\\"")
```

**预期回响**: `他说"好"`

**解析**: \\" 是双引号转义，在字符串中表示一个字面量双引号。

**魔法解析**: 逃脱符 \\ 告诉 Python：后面的这个字符不是语法符号，而是普通字符。\\" 表示"我要的是一个普通的双引号字符，不是咒文的结束"。

**诺克斯指引**:
  引导: \\" 在字符串中就是一个普通的 " 字符。
  朋友: 想在字符串里放双引号？用 \\" 就行。反斜杠说：嘿，后面的这个引号不是结束，是内容。
  明示: 输出 "他说\\"好\\""

**常见反噬**:
  - "认为报错": \\" 是合法的转移序列

---

## bk_esc_2 (code_fill)

**叙事标题**: 亲自使用逃脱符

**叙事描述**: 写一句源语，让虚空显现：他说"好"。提示：需要在咒文内部的双引号前加逃脱符。

**题目**: 打印包含双引号的字符串：他说"好"。

**初始源语**:
```python
# 打印 他说"好"
print("")
```

**正确源语**:
```python
print("他说\\"好\\"")
```

**预期回响**: `他说"好"`

**解析**: 在字符串内部的双引号前加 \\ 来转义。

**魔法解析**: 当字符串内部需要包含双引号时，在每个内部双引号前加 \\。也可以用单引号包裹外层，这样内部就可以直接用双引号。

**诺克斯指引**:
  引导: 内部引号前加反斜杠。
  朋友: 这题确实有点绕。简单说：想在双引号内部再放一个双引号，就在它前面加个 \\。像 \\" 这样。
  明示: print("他说\\"好\\"")

---

## bk_fs_1 (code_fill)

**叙事标题**: 将数字嵌入咒文

**叙事描述**: 有一个容器 v 装了 42。用编织术将其嵌入一句咒文中，显现 "值是 42"。

**题目**: 用 f-string 输出 "值是 42"。

**初始源语**:
```python
v = 42
# 输出 "值是 42"

```

**正确源语**:
```python
v = 42
print(f"值是 {v}")
```

**预期回响**: `值是 42`

**解析**: f-string 的 {} 中可以嵌入变量。

**魔法解析**: 编织术（f-string）可以在咒文中嵌入符文容器的内容。在大括号 {} 中写上容器名即可。

**诺克斯指引**:
  引导: f 前缀 + 大括号。
  朋友: 我刚开始用 f-string 的时候也经常忘加 f。f 前缀告诉 Python：这个咒文里有东西要替换。
  明示: print(f"值是 {v}")

**常见反噬**:
  - "忘记加 f": 没有 f 前缀就不会替换变量

---

## bk_fs_2 (output_predict)

**叙事标题**: 预测编织术的结果

**叙事描述**: 贤者编织了一段咒文，预测它显现的内容。

**题目**: 预测输出。

**选项**:
  0. 5的平方是5*5 
  1. 5的平方是25 
  2. {x}的平方是{x*x} 
  3. 报错 

**初始源语**:
```python
x = 5
print(f"{x}的平方是{x*x}")
```

**预期回响**: `5的平方是25`

**解析**: f-string 的 {} 中不仅可以放变量，还可以放表达式。

**魔法解析**: 编织术中，大括号内不仅可以放容器名，还可以放运算式——{x*x} 会先计算 x 的平方，再嵌入。

**诺克斯指引**:
  引导: 大括号里可以放计算式。
  朋友: 嘿嘿，f-string 可不只能放变量——{x*x} 会先算出 25 再放进去。这就是编织术的厉害之处。
  明示: x=5, x*x=25, 所以输出 "5的平方是25"

**常见反噬**:
  - "认为显示 {x*x}": 不加 f 才会原样显示大括号

---

## bk_in_1 (code_fill)

**叙事标题**: 聆听外界的声音

**叙事描述**: 用聆听术接收一句呼唤，然后用编织术显现 "你输入的是：{呼唤}"。

**题目**: 读取一个单词，用 f-string 输出 "你输入的是：{单词}"。

**初始源语**:
```python
# 读取输入，输出 "你输入的是：{输入内容}"
w = input()

```

**正确源语**:
```python
w = input()
print(f"你输入的是：{w}")
```

**解析**: input() 读取输入，f-string 嵌入输出。

**魔法解析**: 聆听术（input）可以从外界接收信息，但它接收到的永远是咒文（字符串）形态。需要用容器接住它。

**诺克斯指引**:
  引导: 用 input() 接收，用 f 输出。
  朋友: input() 就像对着魔法通道喊了一声，通道那边会传回一句话。记得用容器接住它。
  明示: w = input()
print(f"你输入的是：{w}")

**常见反噬**:
  - "w = input 少括号": input 是函数，需要括号

---

## bk_in_2 (code_fix)

**叙事标题**: 聆听的陷阱

**叙事描述**: 这段源语读取了两个数字并试图相加，但结果不对——因为聆听术传来的东西都是咒文形态。修复它。

**题目**: 读取两个数字并输出它们的和。

**初始源语**:
```python
a = input()
b = input()
print(a + b)
```

**正确源语**:
```python
a = int(input())
b = int(input())
print(a + b)
```

**解析**: input() 返回字符串，需要 int() 转换后才能做数学加法。

**魔法解析**: input() 传回的是咒文（字符串），不是数字。即使你输入了 "3"，它也是咒文形态。需要用 int() 转化为数字才能做加法。

**诺克斯指引**:
  引导: int(input()) 两步结合。
  朋友: 嘿，这题我太熟了——第一节课就踩了坑。input() 传回来的不是数字，是字符串。用 int() 包一层就好了。
  明示: a = int(input())
b = int(input())
print(a + b)

**常见反噬**:
  - "input 结果直接相加": input 返回的是字符串，不是数字

---

## bk_name_1 (choice)

**叙事标题**: 容器命名公约

**叙事描述**: 在源语世界，容器命名有约定俗成的规则。哪个容器名符合最广泛接受的法则？

**题目**: 以下哪个变量名符合 PEP8 规范？

**选项**:
  0. userName 
  1. user_name 
  2. UserName 
  3. username 

**解析**: PEP8 推荐使用蛇形命名法（snake_case）：user_name。

**魔法解析**: PEP8 是 Python 世界的命名公约。推荐使用蛇形命名法（snake_case）：单词之间用下划线连接，全部小写。

**诺克斯指引**:
  引导: 单词间用下划线。
  朋友: Haha，userName 在 JavaScript 里是标准，但在 Python 里得写成 user_name。PEP8 说了算。
  明示: user_name 是 PEP8 推荐的蛇形命名法。

**常见反噬**:
  - "选 userName": 驼峰命名在 Java 中常见，Python 推荐 snake_case

---

## bk_name_2 (code_fix)

**叙事标题**: 错误的容器名

**叙事描述**: 这段源语试图用一个以数字开头的容器名——这在源语世界是不允许的。修复它。

**题目**: 修复非法变量名。

**初始源语**:
```python
1st_place = "gold"
```

**正确源语**:
```python
first_place = "gold"
```

**解析**: 变量名不能以数字开头。

**魔法解析**: 容器名不能以数字开头，不能包含除下划线以外的特殊符号，也不能使用源语关键字（如 class、def）。

**诺克斯指引**:
  引导: 数字不能开头。
  朋友: 1st_place 看起来挺合理的？但 Python 不认——容器名不能用数字开头。改成 first_place 就可以了。
  明示: 改成 first_place = "gold"

**常见反噬**:
  - "1st_place": 变量名以数字开头

---

## bk_op_1 (code_fill)

**叙事标题**: 法阵材料折扣

**叙事描述**: 一件材料原价 200 金币，店主给八折优惠。计算折后的价格。

**题目**: 原价 200，打 8 折，计算折后价。

**初始源语**:
```python
# 原价 200，打 8 折，计算折后价
price = 
print(price)
```

**正确源语**:
```python
price = 200 * 0.8
print(price)
```

**预期回响**: `160.0`

**解析**: 200 * 0.8 = 160，乘法用于计算百分比。

**魔法解析**: 乘法 * 可以计算百分比。200 * 0.8 就是打八折后的价格。

**诺克斯指引**:
  引导: 八折就是乘以 0.8。
  朋友: 八折就是原价乘以 0.8，别搞成除以 8 了。我当年就吃过这个亏，多付了钱。
  明示: 200 * 0.8 = 160

**常见反噬**:
  - "200 / 8": 打折是乘以折扣，不是除以折扣数

---

## bk_op_2 (output_predict)

**叙事标题**: 术式执行顺序

**叙事描述**: 多种运算组合时，源语有其固定的执行顺序。预测这段术式的结果。

**题目**: 预测输出。

**选项**:
  0. 24 
  1. 4 
  2. -4 
  3. 12 

**初始源语**:
```python
print(10 - 2 * 3)
```

**预期回响**: `4`

**解析**: 先乘后减：2 * 3 = 6，10 - 6 = 4。

**魔法解析**: 源语遵循数学中的运算优先级：先乘除后加减。如果希望改变顺序，需要用括号 () 包裹优先计算的部分。

**诺克斯指引**:
  引导: 先乘除后加减。
  朋友: 这题我当年也做错过。Python 和数学课上学的一样——乘除优先。2 * 3 先算，再减。
  明示: 2 * 3 = 6, 10 - 6 = 4

**常见反噬**:
  - "从左到右计算": Python 不会从左到右硬算

---

## bk_print_1 (code_fill)

**叙事标题**: 显化术初演

**叙事描述**: 贤者，你需要让虚空显现数字 42。用最基础的显化咒语来完成。

**题目**: 使用 print() 输出数字 42。

**初始源语**:
```python
# 让虚空显现数字 42

```

**正确源语**:
```python
print(42)
```

**预期回响**: `42`

**解析**: print() 可以直接输出数字，不需要引号。

**魔法解析**: 显化术（print）是最基础的源语指令，它能让虚空浮现你指定的内容。数字不需要引号包裹。

**诺克斯指引**:
  引导: 数字不用加引号。
  朋友: 哈哈，简单吧？print 就是最基础的显化术。数字直接写，不用加引号。
  明示: print(42)

**常见反噬**:
  - "print(42 少括号": print() 的括号必须成对
  - "print("42")": 加了引号输出的是字符串，不是数字

---

## bk_print_2 (code_fill)

**叙事标题**: 同时呼唤两个符文

**叙事描述**: 同时显现咒文 "Python" 和数字 3，让它们之间有一个空格隔开。

**题目**: 用 print() 输出 "Python" 和 3，中间用空格隔开。

**初始源语**:
```python
# 显现 "Python" 和 3

```

**正确源语**:
```python
print("Python", 3)
```

**预期回响**: `Python 3`

**解析**: print() 可以同时输出多个值，默认用空格分隔。

**魔法解析**: 显化术可以同时呼唤多个对象，它们之间会自动出现一个空白间隙。

**诺克斯指引**:
  引导: 多个输出用逗号隔开。
  朋友: 我当年也试过用加号，然后报错了。用逗号就行，print 会自动加空格。
  明示: print("Python", 3)

**常见反噬**:
  - "print("Python" + 3)": 字符串和数字不能直接加

---

## bk_sep_1 (code_fill)

**叙事标题**: 定制显现咒文的间隙

**叙事描述**: 多个符文同时显现时，默认用空白间隙隔开。现在你需要用 "->" 作为间隙来连接 X、Y、Z。

**题目**: 用 "->" 分隔输出 X、Y、Z。

**初始源语**:
```python
# 用 -> 分隔输出 X Y Z
print("", "", "", sep="->")
```

**正确源语**:
```python
print("X", "Y", "Z", sep="->")
```

**预期回响**: `X->Y->Z`

**解析**: sep 参数设置输出值之间的分隔符。

**魔法解析**: sep 参数可以定制显现时多个对象之间的分隔符。默认是空格，可以改成任意字符串。

**诺克斯指引**:
  引导: sep 控制分隔符。
  朋友: print 默认用空格当分隔符。想换的话，sep="->" 就行了。sep 就是 separator 的缩写。
  明示: print("X", "Y", "Z", sep="->")

**常见反噬**:
  - "忘记写 sep": 不写 sep 默认是空格

---

## bk_sep_2 (code_fill)

**叙事标题**: 连续咒文不换气

**叙事描述**: 每次显化后源语会自动换气（换行）。如何让两次显化在同一行连续显现？

**题目**: 让两个 print 在同一行输出。

**初始源语**:
```python
# 让 A 和 B 在同一行
print("A", end="")

```

**正确源语**:
```python
print("A", end="")
print("B")
```

**预期回响**: `AB`

**解析**: end="" 让 print 末尾不加换行，下一个 print 在同一行继续。

**魔法解析**: end 参数控制显现结束时的行为。默认是 \
（换行），改成空字符串 end="" 就可以不换行。

**诺克斯指引**:
  引导: end="" 取消换行。
  朋友: print 每次念完咒语都会换一口气（换行）。不想换气就在括号里加 end=""。
  明示: print("A", end="")
print("B")

**常见反噬**:
  - "end="" 忘记写": 默认换行，不加 end 就会换行

---

## bk_str_1 (code_fill)

**叙事标题**: 用乘法复制咒文

**叙事描述**: "Hi" 是一段短咒文，用复制术 * 将其变为三倍，然后显现。

**题目**: 用 * 将 "Hi" 重复 3 次并打印。

**初始源语**:
```python
# 用 * 将 "Hi" 重复 3 次
print("" * )
```

**正确源语**:
```python
print("Hi" * 3)
```

**预期回响**: `HiHiHi`

**解析**: 字符串 * n 将字符串重复 n 次。

**魔法解析**: 咒文 * 数字可以将咒文重复多次。这是 Python 独有的语法糖——在其他语言中通常需要用循环实现。

**诺克斯指引**:
  引导: 咒文 * 数字。
  朋友: 这个特性我第一次见到的时候觉得好神奇——字符串也能用乘法的？试试 "Hi" * 3，结果是 "HiHiHi"。
  明示: "Hi" * 3 = "HiHiHi"

**常见反噬**:
  - ""Hi" * "3"": 倍数必须是数字，不是字符串

---

## bk_str_2 (code_fill)

**叙事标题**: 测量咒文的长度

**叙事描述**: "Python" 这段咒文有几个符文？用测量术 len() 计算。

**题目**: 打印 "Python" 的长度。

**初始源语**:
```python
# 用 len() 测量字符串长度

```

**正确源语**:
```python
print(len("Python"))
```

**预期回响**: `6`

**解析**: len() 返回字符串的字符数。"Python" 有 6 个字符。

**魔法解析**: len() 是测量术，可以计算咒文（字符串）的长度——也就是它包含多少个字符。注意：中文也是一个字符。

**诺克斯指引**:
  引导: len() 测量长度。
  朋友: len 就是 length 的缩写。想知道一段咒文多长？len("Python") 告诉你 6。
  明示: len("Python") = 6

**常见反噬**:
  - "len 写成 lne": 函数名拼写错误

---

## bk_type_1 (choice)

**叙事标题**: 鉴定元素的种类

**叙事描述**: type() 是鉴定术，可以查看元素的本质。True 是什么类型的元素？

**题目**: type(True) 的结果是什么？

**选项**:
  0. "<class 'int'>" 
  1. "<class 'bool'>" 
  2. "<class 'str'>" 
  3. True 

**解析**: True 是 bool 类型。bool 是 int 的子类。

**魔法解析**: type() 鉴定术返回元素类型的名称。True 是布尔类型（bool），表示真值。有趣的是，在 Python 中 bool 是 int 的子类。

**诺克斯指引**:
  引导: True 不是字符串也不是数字。
  朋友: True 和 False 在 Python 里是特殊的布尔类型。用 type() 一看就知道了。

**常见反噬**:
  - "选择 True": type() 返回的是类型对象，不是值本身

---

## bk_type_2 (output_predict)

**叙事标题**: 预测鉴定术的结果

**叙事描述**: "42" 带了一对引号——预测鉴定术 type() 会给出什么结果。

**题目**: 预测输出。

**选项**:
  0. "<class 'int'>" 
  1. "<class 'str'>" 
  2. 42 
  3. 42 

**初始源语**:
```python
print(type("42"))
```

**解析**: "42" 用引号包裹，是字符串类型。

**魔法解析**: 引号是关键——有引号包裹的就是咒文（字符串），即使里面写的是数字。type() 可以明确区分。

**诺克斯指引**:
  引导: 有引号就是字符串。
  朋友: 这题考的是引号的作用。42 没引号是数字，"42" 有引号就是字符串。type() 一看就明白了。
  明示: "42" 有引号，所以是字符串类型。

---

## bk_var_1 (code_fill)

**叙事标题**: 封印数字到容器

**叙事描述**: 将数字 100 封印到一个名为 score 的容器中，然后显化它。

**题目**: 将值 100 赋给变量 score 并打印。

**初始源语**:
```python
# 将数字 100 封印到 score
score = 
print(score)
```

**正确源语**:
```python
score = 100
print(score)
```

**预期回响**: `100`

**解析**: 变量用 = 赋值，print() 输出变量值。

**魔法解析**: 符文容器（变量）用来存放值。用 = 将右边的值封印到左边的名字中，然后用显化术查看内容。

**诺克斯指引**:
  引导: 等号右边是值，左边是名字。
  朋友: score = 100 就是把 100 这个数字放进名为 score 的容器里。然后用 print 打开盖子看看。
  明示: score = 100
print(score)

**常见反噬**:
  - "score = 100 没加 print": 赋值后需要显化才能看到

---

## bk_var_2 (code_fill)

**叙事标题**: 同名容器的覆盖法则

**叙事描述**: 先将 10 存入容器 x，再覆盖为 20，显化最终结果。

**题目**: 将变量 x 赋值为 10，再赋值为 20，打印 x。

**初始源语**:
```python
# 先将 x 赋值为 10，再改为 20
x = 10

print(x)
```

**正确源语**:
```python
x = 10
x = 20
print(x)
```

**预期回响**: `20`

**解析**: 变量可以重新赋值，新值覆盖旧值。

**魔法解析**: 符文容器可以被覆盖——同一个名字可以指向不同的值。新值会取代旧值。

**诺克斯指引**:
  引导: 后面的赋值会覆盖前面的。
  朋友: 容器 x 先装了 10，又装了 20——盖子一盖，20 留在里面。所以 print(x) 显示 20。
  明示: x = 10
x = 20
print(x)

**常见反噬**:
  - "两次赋值后忘记更新": 最后赋的值才是最终值

---