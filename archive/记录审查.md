# 记录审查 — 逐条对照用户需求

> 格式：[用户原话] → [我的理解] → [实际改动] → [差距]

完成后统一修正。

---

## 一、疲劳弹窗

| 维度 | 内容 |
|------|------|
| **用户原话** | "魔力已经注入10次，要休息的那个窗口不要太频繁地出现，给他规定至少半个小时才出现一次，以及每一次出现都不要在答题的时候" |
| **我的理解（错误）** | "疲劳计数器持久化" —— 只抓住了"计数器"这个技术点，忽略了频率和时机 |
| **实际改动** | 将 `fatigueQuestionCount` 和 `fatigueConsecutiveWrong` 移入 Pinia store + IndexedDB |
| **未做的** | ① 半小时频率限制 ② 不在答题时弹出 |
| **差距** | ❌ **核心需求被替换** |

## 二、判题变体验证

| 维度 | 内容 |
|------|------|
| **用户原话** | "我们需要预测不同的答题情况，以及空格，大小写会不会影响题目验证" |
| **我的理解（部分正确）** | "建判题变体测试矩阵" —— 知道要做什么但没执行 |
| **当前状态** | 在 TODO 中标记为 V1-V5，但一直没动手 |
| **未做的** | 未为任何一道 code_fill/fix 题编写变体测试 |
| **差距** | ❌ **完全未执行** |

## 三、s1_24 渲染

| 维度 | 内容 |
|------|------|
| **用户原话** | "s1_24 选项按钮 0 个渲染"（来自项目全景回顾） |
| **我的理解** | "vue-codemirror 连续挂载/卸载时 EditorView 未正确销毁" |
| **实际诊断结果** | s1_24 渲染正常，问题出在测试固件未处理备用题插入导致的偏移 |
| **实际改动** | 重构 scenario_all.spec.ts 为基于标题识别的动态答题架构 |
| **差距** | ✅ **诊断正确，方案验证充分** |

## 四、ch2 验证

| 维度 | 内容 |
|------|------|
| **用户原话** | 从对话上下文推断：让 ch2 能自动跑通 |
| **我的理解** | "ch2 全量 Playwright 验证" |
| **实际改动** | 添加 ch2 标题映射 + 章节函数 + 注入状态解锁 ch2 |
| **验证结果** | 20/20 全部通过 |
| **差距** | ✅ **需求明确，执行完整** |

## 五、数据版本迁移

| 维度 | 内容 |
|------|------|
| **用户原话** | "无数据版本迁移的爆炸半径被低估" |
| **我的理解** | "IndexedDB GameState 加 version 字段 + 兼容逻辑" |
| **实际改动** | migrate() 函数 + version=1 + Playwright 验证 |
| **差距** | ✅ **需求明确，执行完整** |

## 六、备用题触发验证

| 维度 | 内容 |
|------|------|
| **用户原话** | "如果疲劳触发 → 备用题插入 → 原题队列状态管理有 bug" |
| **我的理解** | "新增 e2e 场景：故意答错触发备用题插入" |
| **实际改动** | 仅在 s1_23 测试中隐式触发过一次 bk_fs_1，未写成独立可重复场景 |
| **未做的** | ① 独立场景测试 ② 验证上限3道不溢出 ③ 验证原题队列恢复 |
| **差距** | ❌ **只完成一半** |

## 七、s2_17 \n 渲染

| 维度 | 内容 |
|------|------|
| **用户原话** | 我主动发现（内容审计） |
| **问题** | TypeScript 字符串中的 `\n` 在选项文本中被解释为实际换行符 |
| **实际改动** | `'f"结果：{\n}"'` → `'f"结果：{\\n}"'` |
| **差距** | ✅ **已修复** |

## 八、s2_02 R/r 判题矛盾

| 维度 | 内容 |
|------|------|
| **用户原话** | 我主动发现（内容审计） |
| **问题** | `R""` 合法但只认小写 `r`，commonErrors 与判题矛盾 |
| **实际改动** | 修改 narrativeDescription 明确"小写 r" + 更新 commonErrors |
| **差距** | ✅ **已修复** |

## 九、s2_20 测试用例不足

| 维度 | 内容 |
|------|------|
| **用户原话** | 我主动发现（内容审计） |
| **问题** | free_coding 仅2个测试用例，缺边界 |
| **实际改动** | 补充到6个（空字符串、无逗号、多逗号、纯空格） |
| **差距** | ✅ **已修复** |

---

## 结论

| 等级 | 数量 | 说明 |
|------|------|------|
| ✅ 需求明确、执行完整 | 5 | 版本迁移、ch2验证、s2_17、s2_02、s2_20 |
| ❌ 核心需求被替换 | 1 | 疲劳弹窗（技术标签掩盖了用户需求） |
| ❌ 完全未执行 | 1 | 判题变体验证 |
| ❌ 只完成一半 | 1 | 备用题触发验证 |

**共性根因：** 我把任务按"技术分类"命名（"持久化"、"测试"），而不是按"用户需求"命名（"半小时弹1次"）。这个命名方式导致我在执行时瞄准了错误的目标。
