# 已知缺口 — 跨会话持久追踪

> 记录所有用户提出但未完成的需求、我知道但未做的验证、发现但未修的隐患。
> **每个缺口包含来源、优先级、状态**，新会话先读此文件。

---

## 一、未完成项

| # | 事项 | 优先级 | 状态 | 修复方向 |
|---|------|--------|------|---------|
| R3 | **开场介绍 P0-3**：IntroOverlay.vue | P3 | 📝 | 纯体验，内容稳定后设计 |
| T1 | **free_coding CodeMirror 输入** — keyboard.type 不处理 `\n` | P2 | 📝 | 逐行 type + Enter 或 clipboard paste |
| E1 | **ch8-ch10 title-based e2e** — 当前只有 fixture 占位 | P1 | 📝 | 加 TITLE_MAP 和 runSection |
| E2 | **备用题生成 ch3-ch10** — 每个 knowledgeTag 缺 2 道 | P1 | 📝 | 按题目设计规范生成 |
| I5 | **CI 配置** — GitHub Actions + headless e2e | P2 | 📝 | |
| I6 | **常量集中** — magic numbers → constants/ | P2 | 📝 | review §3.4 表格 |
| I7 | **死代码清理** — commonErrors.ts 去留决策 | P2 | 📝 | |
| I8 | **大文件拆分** — ChapterSelect/WisdomBook/useQuiz | P3 | 📝 | |
| C7 | **commonErrors.ts 未接入** — detectCommonErrors 只被测试引用 | P3 | 📝 | 作为 feature request 单独评估 |

## 二、已完成

| # | 事项 | 完成方式 | 
|---|------|---------|
| R1 | 判题变体验证 | ✅ 14 变体 × 3 题 E2E 矩阵 + vitest 纯函数 |
| R2 | 备用题触发验证 | ✅ 独立 e2e 场景（scenario_backup.spec.ts） |
| R4 | ChapterSelect 去硬编码 | ✅ displayName/regionName/regionOrder → 数据驱动 |
| R5 | 虚假测试重写 | ✅ 删除 gameStore/e2e_flow 测试；纯函数测试移到 utils/ |
| D1 | s2_17 `\n` 渲染 | ✅ 改为 `\\n` |
| D2 | s2_02 R/r 判题 | ✅ 改描述明确要求小写 r，更新 commonErrors |
| D3 | s2_20 测试用例 | ✅ 2→6 个（含空字符串、无逗号、多逗号等边界） |
| I1 | 错误监控 | ✅ window.onerror + app.config.errorHandler + IndexedDB error_log |
| I2 | Pyodide 加载提示 | ✅ 15 秒后显示"首次加载需下载约 10MB"提示 |
| — | save() 防崩溃 | ✅ try-catch 包裹 |
| — | QuizPanel 拆分 | ✅ 473→137 行，逻辑移到 useQuiz composable |
| — | grade() 提取 | ✅ utils/grade.ts |
| — | 疲劳弹窗 | ✅ 45 分钟累计 + 非答题页弹 |
| — | 数据版本迁移 | ✅ version + migrate() + Playwright 验证 |
| — | PowerShell 写测试文件损坏 | ✅ 禁用 PowerShell 改 .ts/.vue；改用 Write tool 或 Python |
| — | title-based 测试重建 | ✅ 单节独立测试 + Pinia 注入 + 标题识别 |
| — | 同区域章节 displayName 冲突 | ✅ 规范：多章同区域用"区域 · 主题" |
| — | coverage 阈值 + include/exclude | ✅ vitest.config.ts 已修复 |
| — | useQuiz.test.ts 虚假覆盖 | ✅ 改为 import 真实导出 |
| — | restored_test.ts 临时文件 | ✅ 已删除 |
| C1 | verify_questions B2 bug | ✅ 改用章节级 regex + FILES 补全 ch4-6 |
| C2 | stdoutBuffer 模块级变量 | ✅ 移入函数作用域，每次 runPython 独立 setStdout |
| C4 | migrate saved: Record\<string, any\> | ✅ 改为 `migrate(raw: unknown)` + 类型守卫 |
| C6 | gameStore 死导出 | ✅ 已在 P2-5 中从 return 移除 |
| — | 设计规格同步（7 项） | ✅ 数据模型/测试数/验证步骤/ChapterSelect/Store/any/toBeDefined |

## 三、基础设施缺口

| # | 缺口 | 影响 | 优先级 | 状态 |
|---|------|------|--------|------|
| I3 | **input()容器限制**：s1_23/s1_25不能用Pyodide自动验证 | 2题只能人工测 | P1 | ✅ 已标记 |
| I4 | **无视觉回归测试**：UI变化无法自动检测 | 像素改动需人工走查 | P3 | 📝 |

## 四、执行原则（自省）

- **用户诉求必须先写入此文件再动作** — 防止跨会话丢失
- **每轮结束前检查此文件** — 把未完成项带到下一轮
- **新会话第一步读此文件** — 否则可能重蹈"不知道用户要什么"的覆辙

---

*最后更新：2026-06-24 | 第二轮审查后更新*
