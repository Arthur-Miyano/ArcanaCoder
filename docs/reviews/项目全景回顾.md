# 项目全景回顾

> 日期：2026-06-23 | 对话时长：约 8 小时

---

## 一、遇到的问题（完整清单）

### A. 数据层问题

| # | 问题 | 发现方式 | 状态 | 记录位置 |
|---|------|---------|------|---------|
| A1 | s1_20 选项[1]和[2]渲染后视觉相同（`\n` 被 HTML 折叠） | 用户手动测试发现 | ✅ **已修复** — `whitespace-pre-wrap` | 方案 v1-v5 |
| A2 | s1_24 选项按钮 0 个渲染 | Playwright 场景 A 发现 | 🔴 **未解决** — 排查记录：已排除组件复用（:key无效）、数据正确（options 4项）、同节内更早的 s1_20 正常渲染。最可能根因是 vue-codemirror 连续挂载/卸载时 EditorView 未正确销毁，导致第 5 次挂载 OutputPredict 时渲染异常。需隔离测试确认。 | 方案_s1_23_s1_24修复.md |
| A3 | s1_23 在 Pyodide 中 `input()` 无 stdin 导致判错 | Playwright 场景 A 发现 | 📝 **已记录** — 测试框架固有局限 | 方案_s1_23_s1_24修复.md |
| A4 | `validationMode: 'contains'` 字段未在 `validateCode` 中实现 | 用户反馈+代码审查发现 | ✅ **已修复** | QuizPanel.vue:107 |
| A5 | s1_24 数据中的 `validationMode` 字段冗余（output_predict 不需要） | 修复审查中发现 | ✅ **已清理** | stage1_questions.ts |

### B. 代码质量问题

| # | 问题 | 发现方式 | 状态 | 记录位置 |
|---|------|---------|------|---------|
| B1 | `ch2_lists` 占位数据与新区划编号冲突 | 方案设计中发现 | ✅ **已删除** | questions.ts |
| B2 | pyodide.ts 超时消息硬编码（10 秒） | 用户审查清单中发现 | ✅ **已修复** — 动态模板字符串 | pyodide.ts:87 |
| B3 | questions.ts filter 泛型过于复杂 | 用户审查清单中发现 | ✅ **已简化** | questions.ts:77 |
| B4 | storage.ts IndexedDB 无重试机制 | 用户审查清单中发现 | ✅ **已修复** — 最多 3 次重试 | storage.ts |
| B5 | 选项按钮 `data-testid` 缺失 | Playwright 测试需要 | ✅ **已补充** | ChoiceQuestion, OutputPredict, QuizPanel, FeedbackPanel |
| B6 | FeedbackPanel 正答按钮缺 `data-testid` | Playwright 测试 R2 中发现 | ✅ **已补充** | FeedbackPanel.vue |
| B7 | QuizPanel 题型组件缺 `:key` | s1_24 渲染问题分析中发现 | ✅ **已补充** — 所有 3 个组件 | QuizPanel.vue |

### C. 测试基础设施问题

| # | 问题 | 发现方式 | 状态 | 记录位置 |
|---|------|---------|------|---------|
| C1 | 57 个 vitest 测试全是虚假测试（不接触真实数据） | 用户指出 | 📝 **已记录** — 方案_问题复盘与改进机制.md |
| C2 | Pyodide CDN 加载在多次测试中不稳定（3 次后卡 30%） | Playwright 测试 R5/R6 中发现 | ✅ **已解决** — 本地托管 Pyodide | pyodide.ts + public/pyodide/ |
| C3 | 诊断块 `isVisible()` + `textContent()` 干扰 WASM 编译 | 调试中发现 | ✅ **已定位** — 去掉诊断块后正常 | 验证过程 |
| C4 | Playwright 默认超时 30s 不够 | 测试运行中发现 | ✅ **已修正** — 改为 120s | playwright.config.ts |
| C5 | Vite HMR 在测试期间触发全量页面刷新 | 用户测试中发现 | ✅ **已修正** — vite.config.ts 加 watch.ignored | vite.config.ts |
| C6 | 测试中所有 `catch(() => {})` 导致状态丢失 | 开发中发现 | 📝 **已记录** — 需清理 | scenario_all.spec.ts |

### D. 流程与执行问题

| # | 问题 | 发生次数 | 状态 | 记录位置 |
|---|------|---------|------|---------|
| D1 | 需求未确认就动手（写测试前没问目标） | 1 次 | ✅ **已改进** — CLAUDE.md 加执行流程 | CLAUDE.md |
| D2 | 同一方向连败不换方案（R3-R6 连续修浏览器问题） | 4 轮 | ✅ **已改进** — "2 败停"规则 | 方案_问题复盘与改进机制.md |
| D3 | 方案正文与前置验证结果脱节（v1 正文用 fill() 但验证说 type 可用） | 1 次 | ✅ **已改进** — v2 起验证结果先于正文 | 方案_全量测试计划.md |
| D4 | 跳过"先出方案再执行"流程（直接写 verify_cm_input.spec.ts） | 1 次 | ✅ **已改进** — CLAUDE.md 格式头执行 | CLAUDE.md |
| D5 | 在用户测试期间持续改文件（HMR 触发页面刷新） | 多次 | ✅ **已改进** — 方案确认后批量执行 | 复盘记录 |

---

## 二、方案版本演进

| 版本 | 文件 | 关键变更 |
|------|------|---------|
| v1 | 方案_所有问题汇总与修复计划.md | 初始方案，5 个问题 |
| v2 | 同上 | 采纳审查意见：数据备份、验证脚本细化、隐私模式兼容 |
| v3 | 同上 | 补充错误监控/性能基线/数据版本/移动端 |
| — | 方案_内容扩展工作计划.md | 10 章内容扩展计划（独立于本轮） |
| — | 方案_问题复盘与改进机制.md | 虚假测试反思、2 败停规则、动手前 checklist |
| v1 | 方案_全量测试计划.md | 84 题×4 场景，一次性执行 |
| v2 | 同上 | 按节拆分，分阶段，备用题数学修正（5+3=8） |
| v1 | 方案_Playwright测试扩展计划.md | 5 题 MVP，fill() 方案 |
| v2 | 同上 | keyboard.type 方案，导航选择器验证 |
| v3 | 同上 | beforeEach 顺序修正、text=目录 验证、开始试炼后等待 |
| — | 方案_完整用户流程与Playwright修正方案.md | 用户操作流程 12 步记录 |
| — | 方案_s1_23_s1_24修复.md | 双题修复方案（已确认待执行的部分） |

**总计：3 个主方案文档，约 13 次版本迭代。**

已废弃/删除的方案：`方案_主题题库修复.md`、`方案_Phase1收尾.md`、`题目审核清单_完整版.md`、`audit_report.md`（已归档）。

---

## 三、任务完成情况

### 原方案各 Phase 状态

| Phase | 目标 | 状态 | 完成度 |
|-------|------|------|--------|
| 1 | 内容体系重建（10 章规划） | ✅ **完成** | 设计规格.md 已更新 |
| 2 | 项目清理（删除误导文件） | ✅ **完成** | 4 删 2 归档 |
| 3 | 基础设施（Pyodide 本地化、Playwright 配置） | ✅ **完成** | public/pyodide/, playwright.config.ts |
| 4 | P0-1 选项显示修复 | ✅ **完成** | whitespace-pre-wrap |
| 5 | P0-2 数据静态核查 | ✅ **完成** | verify_questions.cjs, 84 题 0 错误 |
| 6 | P0-2 数据动态验证（Step B） | 🔄 **部分完成** | Playwright 场景 A-C 通过，D 通过 |
| 7 | P1-4 疲劳计数器 | 🔴 **未开始** | — |
| 8 | P0-3 开场介绍 | 🔴 **未开始** | — |
| 9 | 遗漏项 A/B/C（错误监控/性能基线/数据版本） | 🔴 **未开始** | — |

### 测试覆盖统计

| 测试维度 | 覆盖 | 说明 |
|---------|------|------|
| Vitest 单元测试 | 57/57 ✅ | 未接触真实数据，判定为虚假测试 |
| Playwright 场景 A（全对） | 22/24 ✅ | s1_23(input 局限)、s1_24(渲染 bug) 未通过 |
| Playwright 场景 B（全错） | 通过 ✅ | 备用题上限 3 验证 |
| Playwright 场景 C（混合） | 通过 ✅ | 错-错-对-错-错，总题数 8 验证 |
| Playwright 场景 D（变体） | 通过 ✅ | 空格/换行容错验证 |
| ch2_strings（20 题） | ❌ 未测 | 被 s1_24 阻塞（ch1 无法完整通关） |
| backup_questions（36 题） | ❌ 未测 | 需答错触发，可基于场景 B 扩展 |

---

## 四、当前产出物清单

### 测试代码

```
e2e/
  fixtures/               ← 7 个节 JSON + all_questions.json（84 题）
  results/                ← 场景 A/B/C/D + all 结果 JSON
  tests/
    scenario_A_all.spec.ts  ← ch1 全对验证（被 scenario_all 替代）
    scenario_all.spec.ts    ← 当前主测试：ch1 全量
    scenario_B.spec.ts      ← 全错+备用题
    scenario_C.spec.ts      ← 混合对错
    scenario_D.spec.ts      ← 代码变体
  poc.spec.ts               ← 初始验证（已删除）
  s1_vars.spec.ts           ← 初始 5 题（已删除）
  verify_cm_input.spec.ts   ← CM6 验证（已删除）
playwright.config.ts
scripts/
  extract_fixtures.ts       ← 从 TS 源码提取 fixture JSON
  verify_questions.cjs      ← 静态数据核查
```

### 配置改动

```
vite.config.ts            ← server.watch.ignored + define
pyodide.ts                ← 本地/CDN 环境切换
playwright.config.ts      ← timeout 120s, workers 1, 真实 UA
vitest.config.ts          ← exclude e2e/
```

### 组件改动

```
ChoiceQuestion.vue    ← whitespace-pre-wrap, data-testid
OutputPredict.vue     ← whitespace-pre-wrap, data-testid
QuizPanel.vue         ← data-testid, data-executing, :key, validationMode
FeedbackPanel.vue     ← data-testid (feedback-panel, btn-next)
ChapterSelect.vue     ← 未改（硬编码待迁）
gameStore.ts          ← load 字段级合并防缺损
storage.ts            ← IndexedDB 重试
DailyPlanDialog.vue   ← knowledgeStates 空值防御
```

---

## 五、未解决问题

| 优先级 | 问题 | 阻塞什么 | 建议处理方式 |
|--------|------|---------|------------|
| P0 | s1_24 OutputPredict 选项不渲染 | ch2 解锁、全量测试通过 | 隔离排查：单独渲染 s1_24 确认复现条件 |
| P1 | s1_23 input() 在 Pyodide 中不可用 | 该题无法自动化验证 | 标记为"容器测试不可覆盖"，人工验证 |
| P1 | ch2_strings + backup 全量未测 | 覆盖率不全 | 修复 s1_24 后自动解锁 |
| P2 | `catch(() => {})` 在测试中状态丢失 | 测试可维护性 | 逐步替换为显式错误处理 |
| P2 | validationMode 仅在 code_fill/fix 中实现，contains 在 free_coding 未实现 | free_coding 容错 | 后续扩展 |
| P3 | 疲劳计数器异常 | 用户体验 | 需在浏览器中复现后排查 |
| P3 | 开场介绍 P0-3 | 用户体验 | 按设计规格方案实现 |

---

## 六、方案归档

所有旧方案已清理：

| 位置 | 内容 | 当前状态 |
|------|------|---------|
| 项目根目录 | 6 个活动方案文档 | ✅ 当前有效 |
| `archive/` | 5 个 Phase 1 历史方案 | ✅ 已归档，不干扰 |
| CLAUDE.md | 执行流程规范 | ✅ 已写入 |
| CHANGELOG.md | 设计决策变更记录 | ✅ 已更新 |
| 方案_问题复盘与改进机制.md | 错误反思+改进机制 | ✅ 已记录 |

---

## 七、快速开始：新会话文件索引

以下文件按重要性排序，新会话先读这批就能了解项目全貌。

### 第一梯队：项目骨架（必须读）

| 文件 | 为什么读 |
|------|---------|
| `package.json` | 依赖、脚本命令 |
| `vite.config.ts` | 构建配置、base 路径、watch 忽略 |
| `playwright.config.ts` | 测试配置、timeout、baseURL、workers |
| `tsconfig.json` | TypeScript 路径别名 `@/` → `./src/` |

### 第二梯队：核心逻辑（了解架构）

| 文件 | 作用 |
|------|------|
| `src/App.vue` | 主路由（viewState 切换），项目入口 |
| `src/stores/gameStore.ts` | 全局状态（等级/经验/进度/章节解锁） |
| `src/data/questions.ts` | 章节定义 + 题目数组 + 查询函数 |
| `src/data/stage1_questions.ts` | ch1 28 题原始数据 |
| `src/data/stage2_questions.ts` | ch2 20 题原始数据 |
| `src/data/backup_questions.ts` | 备用题库 36 题 |
| `src/data/wisdom.ts` | 智慧之书各章节知识点 |
| `src/services/pyodide.ts` | Pyodide 加载 + Python 执行（本地/CDN 切换） |
| `src/services/storage.ts` | IndexedDB 持久化（有重试机制） |
| `src/types/index.ts` | 类型定义（Question, Chapter, GameState 等） |

### 第三梯队：组件（按需读）

| 组件 | 职责 |
|------|------|
| `ChapterSelect.vue` | 章节选择和节展开（部分硬编码待迁） |
| `QuizPanel.vue` | 答题面板、题型分发、判题逻辑 |
| `ChoiceQuestion.vue` | 选择题渲染 |
| `CodeQuestion.vue` | CodeMirror 编辑器 |
| `OutputPredict.vue` | 输出预测题（含代码展示+选项） |
| `FeedbackPanel.vue` | 反馈面板（正确/错误/diff/提示） |
| `NoxDialog.vue` | 诺克斯对话气泡 |
| `DailyPlanDialog.vue` | 每日计划弹窗（两步流程） |
| `WisdomBook.vue` | 3D翻页知识书 |
| `LoadingOverlay.vue` | Pyodide 加载进度条 |
| `GameHeader.vue` | 贤者头像+经验条 |
| `ChapterComplete.vue` | 节完成页 |

### 第四梯队：测试和工具

| 文件/目录 | 用途 |
|-----------|------|
| `e2e/tests/scenario_all.spec.ts` | 主测试：ch1 全量 24 题验证 |
| `e2e/tests/scenario_B.spec.ts` | 全错+备用题验证 |
| `e2e/tests/scenario_C.spec.ts` | 混合对错验证 |
| `e2e/tests/scenario_D.spec.ts` | 代码变体容错验证 |
| `e2e/fixtures/` | 各节 fixture JSON + all_questions.json |
| `e2e/results/` | 各场景结果 JSON |
| `scripts/extract_fixtures.ts` | 从 TS 源码提取 fixture |
| `scripts/verify_questions.cjs` | 静态数据核查（84 题 0 错误） |
| `src/__tests__/` | 57 个 vitest 单元测试（不接触真实数据，判定为虚假测试） |

### 方案文档索引

| 文档 | 内容 |
|------|------|
| `方案_所有问题汇总与修复计划.md` v5 | 当前活动方案（问题清单+修复计划+工作顺序） |
| `方案_问题复盘与改进机制.md` | 虚假测试反思、2败停规则、动手前checklist |
| `方案_全量测试计划.md` v2 | 4 场景全量测试分解（按节拆分） |
| `方案_Playwright测试扩展计划.md` v3 | Playwright 配置、CM6 输入方案、导航选择器 |
| `方案_s1_23_s1_24修复.md` | 双题修复方案（部分已执行） |
| `方案_内容扩展工作计划.md` | 10 章内容扩展（独立于本轮，未执行） |
| `项目全景回顾.md` | 本文件，完整回顾 |
