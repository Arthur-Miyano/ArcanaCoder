import type { Question } from '@/types'
export const stage8Questions: Question[] = [
  { id:'s8_01', type:'choice', chapterId:'ch8_annotations', title:'类型注解', narrativeTitle:'类型标注',
    narrativeDesc:'Python 3 支持类型注解——告诉阅读者变量应该是什么类型。哪段代码给变量加了类型注解？',
    narrativeExplanation:'变量: 类型 语法为变量添加类型注解。运行时不影响逻辑。',
    description:'以下哪个正确给变量添加了类型注解？', difficulty:1, knowledgeTags:['类型注解'],
    options:['name: str = "贤者"','str name = "贤者"','name: "贤者"','name = str("贤者")'], correctOption:0,
    explanation:'变量: 类型 = 值 是 Python 类型注解的标准语法。',
    hint:'变量: 类型 = 值', hintRoleplay:'类型注解告诉读者"这个变量应该是字符串类型"。Python 不会强制检查。', hintDirect:'name: str = "贤者"' },

  { id:'s8_02', type:'code_fill', chapterId:'ch8_annotations', title:'参数注解', narrativeTitle:'参数标注',
    narrativeDesc:'函数的参数也可以加类型注解——标明每个参数的类型。',
    narrativeExplanation:'参数名: 类型 标注参数。-> 返回类型 标注返回值。',
    description:'给函数参数 name 加 str 类型注解，返回值加 str 注解。', difficulty:2, knowledgeTags:['类型注解'],
    initialCode:'def greet(name) -> str:\n    return f"你好，{name}"\n',
    correctCode:'def greet(name: str) -> str:\n    return f"你好，{name}"',
    expectedOutput:'', validationMode:'contains',
    explanation:'参数: str 标注参数类型，-> str 标注返回类型。',
    hint:'name: str 和 -> str', hintRoleplay:'参数注解写参数后面加冒号，返回注解用 -> 写在括号后面。', hintDirect:'def greet(name: str) -> str:' },

  { id:'s8_03', type:'code_fill', chapterId:'ch8_annotations', title:'返回类型', narrativeTitle:'返回值标注',
    narrativeDesc:'函数的返回值也可以标注类型。', narrativeExplanation:'-> 类型 标注函数返回值类型。',
    description:'给 add 函数返回值添加 int 类型注解。', difficulty:2, knowledgeTags:['类型注解'],
    initialCode:'def add(a: int, b: int):\n    return a + b\n',
    correctCode:'def add(a: int, b: int) -> int:\n    return a + b',
    expectedOutput:'', validationMode:'contains',
    explanation:'-> int 告诉调用者这个函数返回整数。',
    hint:'-> 返回类型', hintRoleplay:'-> int 就像标签——让调用者知道结果是什么类型。', hintDirect:'-> int:' },

  { id:'s8_04', type:'code_fill', chapterId:'ch8_annotations', title:'列表注解', narrativeTitle:'泛型标注',
    narrativeDesc:'列表和字典等容器类型的注解需要从 typing 导入。',
    narrativeExplanation:'List[int] 表示"整数列表"，Dict[str, int] 表示"字符串到整数字典"。',
    description:'导入 List 类型，给参数 nums 加 List[int] 注解。', difficulty:2, knowledgeTags:['类型注解','List'],
    initialCode:'from typing import List\n\ndef sum_list(nums) -> int:\n    return sum(nums)\n',
    correctCode:'from typing import List\n\ndef sum_list(nums: List[int]) -> int:\n    return sum(nums)',
    expectedOutput:'', validationMode:'contains',
    explanation:'List[int] 表示"元素类型为 int 的列表"。类型注解中的泛型用方括号。',
    hint:'nums: List[int]', hintRoleplay:'List[int] 是"整数列表"。类似地 Dict[str,int] 是"字符串到整数的字典"。', hintDirect:'nums: List[int]' },

  { id:'s8_05', type:'code_fix', chapterId:'ch8_annotations', title:'注解修复', narrativeTitle:'类型纠错',
    narrativeDesc:'这段代码的类型注解有多处错误。', narrativeExplanation:'类型注解语法：变量: 类型、参数: 类型、-> 返回类型。',
    description:'修复类型注解的语法错误。', difficulty:2, knowledgeTags:['类型注解'],
    initialCode:'name: str "贤者"\ndef greet(name) -> :\n    return f"Hi {name}"',
    correctCode:'name: str = "贤者"\ndef greet(name: str) -> str:\n    return f"Hi {name}"',
    expectedOutput:'', validationMode:'contains',
    explanation:'变量注解需要 =，函数参数注解用冒号，返回类型用 -> 类型。',
    hint:'三处错误：缺 =、缺 :str、缺 str。', hintRoleplay:'类型注解有三个地方要注意：变量要 =、参数要 :、返回要 ->。', hintDirect:'name: str = "贤者"' },

  // 第 2 节：类型进阶
  { id:'s8_06', type:'choice', chapterId:'ch8_annotations', title:'可选类型', narrativeTitle:'可选标注',
    narrativeDesc:'函数可能返回 None 应该怎么标注？', narrativeExplanation:'Optional[str] 表示"可能是字符串，也可能是 None"。',
    description:'以下哪个表示"可能返回字符串也可能返回 None"？', difficulty:2, knowledgeTags:['类型注解','Optional'],
    options:['Optional[str]','str | None','Option[str]','str?'], correctOption:0,
    explanation:'Optional[str] 等于 Union[str, None]，表示"str 或 None"。',
    hint:'Optional[类型]', hintRoleplay:'Optional 是"可选"——可能是这个类型，也可能是 None。', hintDirect:'Optional[str]' },

  { id:'s8_07', type:'code_fill', chapterId:'ch8_annotations', title:'Union 类型', narrativeTitle:'联合类型',
    narrativeDesc:'一个变量可能接受多种类型时用 Union。', narrativeExplanation:'Union[str, int] 表示"字符串或整数"。',
    description:'用 Union[str, int] 标注参数 value 的类型。', difficulty:2, knowledgeTags:['类型注解','Union'],
    initialCode:'from typing import Union\n\ndef show(value) -> str:\n    return str(value)\n',
    correctCode:'from typing import Union\n\ndef show(value: Union[str, int]) -> str:\n    return str(value)',
    expectedOutput:'', validationMode:'contains',
    explanation:'Union[类型1, 类型2] 表示接受其中任一类型。',
    hint:'value: Union[str, int]', hintRoleplay:'Union 是"或"——这个参数可以是字符串或者整数。', hintDirect:'value: Union[str, int]' },

  { id:'s8_08', type:'code_fill', chapterId:'ch8_annotations', title:'Any 类型', narrativeTitle:'任意类型',
    narrativeDesc:'确定不了类型时可以用 Any。但尽量少用。', narrativeExplanation:'Any 表示"任意类型"，等价于不做类型检查。',
    description:'用 Any 标注可以接受任意类型的参数。', difficulty:2, knowledgeTags:['类型注解','Any'],
    initialCode:'from typing import Any\n\ndef show(value) -> str:\n    return str(value)\n',
    correctCode:'from typing import Any\n\ndef show(value: Any) -> str:\n    return str(value)',
    expectedOutput:'', validationMode:'contains',
    explanation:'Any 类型不做检查——任何值都可以传入。但能用具体类型时尽量用具体类型。',
    hint:'value: Any', hintRoleplay:'Any 是"随便"——什么类型都行。但最好别偷懒，尽量写具体类型。', hintDirect:'value: Any' },

  { id:'s8_09', type:'code_fix', chapterId:'ch8_annotations', title:'泛型修复', narrativeTitle:'泛型纠错',
    narrativeDesc:'这段类型注解的导入和用法都有问题。', narrativeExplanation:'List、Optional、Union 需要从 typing 导入。',
    description:'修复类型注解。', difficulty:3, knowledgeTags:['类型注解'],
    initialCode:'def process(items: list[str]) -> None:\n    for i in items:\n        print(i)',
    correctCode:'from typing import List\ndef process(items: List[str]) -> None:\n    for i in items:\n        print(i)',
    expectedOutput:'', validationMode:'contains',
    explanation:'Python 3.8 中 List[str] 需要从 typing 导入。3.9+ 可以直接用 list[str]。',
    hint:'加 from typing import List，用 List[str]。', hintRoleplay:'Python 3.9 前泛型要从 typing 导入。3.9+ 可以直接用小写 list。', hintDirect:'from typing import List' },

  { id:'s8_10', type:'output_predict', chapterId:'ch8_annotations', title:'注解不影响运行', narrativeTitle:'运行验证',
    narrativeDesc:'类型注解在运行时没有任何效果。预测输出：', narrativeExplanation:'即使注解写 int，传 string 也能运行。注解只是提示。',
    description:'预测输出。', difficulty:2, knowledgeTags:['类型注解'],
    initialCode:'def add(a: int, b: int) -> int:\n    return a + b\nprint(add("3", "4"))',
    options:['7','"34"','34','TypeError'], correctOption:2,
    expectedOutput:'34',
    explanation:'a: int 不会阻止字符串传入。+ 号对字符串是拼接，输出 "34"。',
    hint:'Python 不强制类型。', hintRoleplay:'类型注解是"建议"不是"强制"。写了 str 但传 int，Python 也不会报错。', hintDirect:'34' },

  // 第 3 节：dataclass
  { id:'s8_11', type:'choice', chapterId:'ch8_annotations', title:'dataclass 语法', narrativeTitle:'数据类',
    narrativeDesc:'@dataclass 自动为类生成 __init__、__repr__、__eq__ 等方法。',
    narrativeExplanation:'@dataclass 装饰器配合类型注解，一行顶十行。',
    description:'以下哪个正确使用了 @dataclass？', difficulty:1, knowledgeTags:['dataclass'],
    options:['@dataclass\\nclass Point:\\n    x: int','@dataclass class Point()','data class Point:','@dataclass()\\ndef Point:'],
    correctOption:0,
    explanation:'@dataclass 装饰在类上，属性用类型注解声明。',
    hint:'@dataclass + class + 属性: 类型', hintRoleplay:'@dataclass 自动帮你写 __init__、__repr__、__eq__。', hintDirect:'@dataclass\\nclass Point:' },

  { id:'s8_12', type:'code_fill', chapterId:'ch8_annotations', title:'定义 dataclass', narrativeTitle:'定义数据类',
    narrativeDesc:'用 @dataclass 定义一个 Person 类，包含 name 和 age 属性。',
    narrativeExplanation:'@dataclass 需要从 dataclasses 导入。属性用类型注解声明。',
    description:'导入 dataclass 并定义 Person 数据类。', difficulty:2, knowledgeTags:['dataclass'],
    initialCode:'from dataclasses import dataclass\n\n@dataclass\nclass Point:\n    x: int\n    y: int\n\np = Point(3, 4)\nprint(p)',
    correctCode:'from dataclasses import dataclass\n\n@dataclass\nclass Point:\n    x: int\n    y: int\n\np = Point(3, 4)\nprint(p)',
    expectedOutput:'Point(x=3, y=4)',
    explanation:'@dataclass 自动生成 __init__ 和 __repr__。print 显示 Point(x=3, y=4)。',
    hint:'@dataclass 自动生成 __repr__。', hintRoleplay:'用 @dataclass，你只需要写属性，__init__ 和 __repr__ 自动生成。', hintDirect:'（直接运行即可）' },

  { id:'s8_13', type:'code_fill', chapterId:'ch8_annotations', title:'dataclass 默认值', narrativeTitle:'默认属性',
    narrativeDesc:'dataclass 的属性可以设置默认值。', narrativeExplanation:'属性: 类型 = 默认值 设置默认属性。',
    description:'给 name 设置默认值 "匿名"，age 设置默认值 0。', difficulty:2, knowledgeTags:['dataclass'],
    initialCode:'from dataclasses import dataclass\n\n@dataclass\nclass User:\n    name: str\n    age: int\n\nu = User()\nprint(u)\n',
    correctCode:'from dataclasses import dataclass\n\n@dataclass\nclass User:\n    name: str = "匿名"\n    age: int = 0\n\nu = User()\nprint(u)',
    expectedOutput:'User(name=匿名, age=0)',
    explanation:'name: str = "匿名" 设置默认值，创建时不传参就用默认值。',
    hint:'属性: 类型 = 默认值', hintRoleplay:'默认值让属性可省略——创建实例时不传就用默认值。', hintDirect:'name: str = "匿名"' },

  { id:'s8_14', type:'code_fix', chapterId:'ch8_annotations', title:'dataclass 修复', narrativeTitle:'数据类纠错',
    narrativeDesc:'这段 dataclass 的使用有多处错误。', narrativeExplanation:'@dataclass 需要在类定义之上，属性需要类型注解。',
    description:'修复 dataclass 定义。', difficulty:2, knowledgeTags:['dataclass'],
    initialCode:'dataclass\nclass Point:\n    x = 0\n    y = 0\n\np = Point()',
    correctCode:'from dataclasses import dataclass\n\n@dataclass\nclass Point:\n    x: int = 0\n    y: int = 0\n\np = Point()',
    expectedOutput:'', validationMode:'contains',
    explanation:'@dataclass 需要 import 和装饰器，属性需要类型注解和可选的默认值。',
    hint:'加 import、加 @、加 : int。', hintRoleplay:'三个问题：没有 import、没有 @、属性没有类型注解。', hintDirect:'from dataclasses import dataclass\\n@dataclass' },

  { id:'s8_15', type:'output_predict', chapterId:'ch8_annotations', title:'dataclass 比较', narrativeTitle:'自动比较',
    narrativeDesc:'@dataclass 自动生成 __eq__。预测输出：', narrativeExplanation:'@dataclass 自动生成 __eq__，两个同值对象相等。',
    description:'预测输出。', difficulty:2, knowledgeTags:['dataclass'],
    initialCode:'from dataclasses import dataclass\n\n@dataclass\nclass Point:\n    x: int\n    y: int\n\np1 = Point(1, 2)\np2 = Point(1, 2)\nprint(p1 == p2)',
    options:['True','False','Point(1,2)','报错'], correctOption:0,
    expectedOutput:'True',
    explanation:'@dataclass 自动生成 __eq__，相同值的两个实例比较为相等。',
    hint:'dataclass 自动比较属性值。', hintRoleplay:'普通类 p1==p2 比较的是内存地址。dataclass 比较的是属性值。', hintDirect:'True' },

  // 第 4 节：上下文管理器
  { id:'s8_16', type:'choice', chapterId:'ch8_annotations', title:'with 语句', narrativeTitle:'资源管理',
    narrativeDesc:'with 语句自动管理资源——进入时调用 __enter__，退出时调用 __exit__。',
    narrativeExplanation:'with 确保资源在使用后自动释放，即使发生异常。',
    description:'以下哪个是 with 语句的正确用法？', difficulty:1, knowledgeTags:['上下文管理器'],
    options:['with open("f.txt") as f:','with open("f.txt")','using open("f.txt")','open("f.txt") as f:'], correctOption:0,
    explanation:'with 表达式 as 变量: 标准写法。as 是可选的。',
    hint:'with ... as 变量:', hintRoleplay:'with 自动调用 __enter__ 和 __exit__，确保资源释放。', hintDirect:'with open("f.txt") as f:' },

  { id:'s8_17', type:'code_fill', chapterId:'ch8_annotations', title:'with open', narrativeTitle:'文件读取',
    narrativeDesc:'用 with open 安全读取文件内容。', narrativeExplanation:'with open 自动关闭文件，不需要手动 f.close()。',
    description:'用 with open 读取并打印文件内容。', difficulty:2, knowledgeTags:['上下文管理器','文件'],
    initialCode:'# 用 with open 读取并打印\nwith open("test.txt") as f:\n    pass\n',
    correctCode:'with open("test.txt") as f:\n    print(f.read())',
    expectedOutput:'', validationMode:'contains',
    explanation:'with open 语句确保文件在使用后自动关闭。',
    hint:'print(f.read())', hintRoleplay:'with open 自动管理文件——不用记得关。', hintDirect:'with open("test.txt") as f:\\n    print(f.read())' },

  { id:'s8_18', type:'code_fill', chapterId:'ch8_annotations', title:'自定义上下文', narrativeTitle:'自定义管理器',
    narrativeDesc:'实现 __enter__ 和 __exit__ 方法创建自定义上下文管理器。', narrativeExplanation:'__enter__ 在进入 with 块时调用，__exit__ 在退出时调用。',
    description:'实现 __enter__ 打印"开始"返回 self，__exit__ 打印"结束"。', difficulty:3, knowledgeTags:['上下文管理器'],
    initialCode:'class Timer:\n    def __enter__(self):\n        # 打印 "开始" 并返回 self\n        pass\n    def __exit__(self, *args):\n        # 打印 "结束"\n        pass\n\nwith Timer():\n    print("工作中")\n',
    correctCode:'class Timer:\n    def __enter__(self):\n        print("开始")\n        return self\n    def __exit__(self, *args):\n        print("结束")\n\nwith Timer():\n    print("工作中")',
    expectedOutput:'开始\n工作中\n结束',
    explanation:'__enter__ 进入 with 时执行，__exit__ 退出 with 时执行（即使异常）。',
    hint:'__enter__ return self，__exit__ 打印。', hintRoleplay:'__enter__ 是"进入时做的事"，__exit__ 是"退出时做的事"——不管有没有异常。', hintDirect:'print("开始")\\nreturn self' },

  { id:'s8_19', type:'code_fix', chapterId:'ch8_annotations', title:'上下文修复', narrativeTitle:'管理器纠错',
    narrativeDesc:'这段上下文管理器缺少关键方法。', narrativeExplanation:'需要实现 __enter__ 和 __exit__。',
    description:'修复使 with 语句能正常工作。', difficulty:2, knowledgeTags:['上下文管理器'],
    initialCode:'class Managed:\n    def enter(self):\n        return self\n    def exit(self):\n        pass\n\nwith Managed() as m:\n    print("ok")',
    correctCode:'class Managed:\n    def __enter__(self):\n        return self\n    def __exit__(self, *args):\n        pass\n\nwith Managed() as m:\n    print("ok")',
    expectedOutput:'ok',
    explanation:'上下文管理器的方法名是 __enter__ 和 __exit__，不是 enter 和 exit。__exit__ 接收 3 个异常参数。',
    hint:'加双下划线，__exit__ 要 *args。', hintRoleplay:'__enter__ 和 __exit__ 是魔术方法——名字固定不能改。', hintDirect:'def __enter__(self): 和 def __exit__(self, *args):' },

  { id:'s8_20', type:'free_coding', chapterId:'ch8_annotations', title:'数据类管理器', narrativeTitle:'综合应用',
    narrativeDesc:'定义一个带类型注解的 dataclass Config，管理一个文件路径。再编写一个上下文管理器 safe_open，自动处理异常。',
    narrativeExplanation:'综合运用类型注解、dataclass、上下文管理器。',
    description:'实现 Config 数据类和 safe_open 上下文管理器。',
    difficulty:4, knowledgeTags:['dataclass','类型注解','上下文管理器','综合'],
    initialCode:'from dataclasses import dataclass\nfrom typing import Optional\n\n@dataclass\nclass Config:\n    path: str\n    encoding: str = "utf-8"\n\ndef safe_open(config: Config):\n    # 实现上下文管理器，返回 open(config.path, encoding=config.encoding)\n    pass\n',
    correctCode:'from dataclasses import dataclass\nfrom typing import Optional\nfrom contextlib import contextmanager\n\n@dataclass\nclass Config:\n    path: str\n    encoding: str = "utf-8"\n\n@contextmanager\ndef safe_open(config: Config):\n    try:\n        f = open(config.path, encoding=config.encoding)\n        yield f\n    finally:\n        f.close()\n',
    testCases:[],  // 综合题，需要环境支持
    explanation:'Config 用 dataclass 定义配置结构。safe_open 用 @contextmanager 简写上下文管理器。',
    hint:'@contextmanager + yield 是上下文管理器的简写方式。', hintRoleplay:'综合题串联了类型注解、dataclass、上下文管理器三节知识。', hintDirect:'@contextmanager\\ndef safe_open(config):\\n    f = open(...)\\n    yield f\\n    f.close()' },
]
