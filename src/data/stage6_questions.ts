import type { Question } from '@/types'

export const stage6Questions: Question[] = [
  // ========== 第 1 节：异常基础（5 题）==========
  { id:'s6_01', type:'choice', chapterId:'ch6_errors', title:'异常捕获', narrativeTitle:'容错术式',
    narrativeDesc:'代码运行中可能出现意外情况。哪段源语能安全捕获异常？', narrativeExplanation:'try-except 捕获并处理异常。',
    description:'以下哪个是 Python 异常捕获的正确语法？', difficulty:1, knowledgeTags:['异常','try','except'],
    options:['try: ... except:','try: ... catch:','try: ... error:','try: ... finally:'], correctOption:0,
    explanation:'Python 用 try-except，不是 try-catch。',
    commonErrors:[{ pattern:'用了 catch', message:'Python 用 except', noxHint:'Python 的异常捕获用 except。' }],
    hint:'try: ... except:', hintRoleplay:'try-except 给代码穿盔甲。', hintDirect:'try: ... except:' },

  { id:'s6_02', type:'code_fill', chapterId:'ch6_errors', title:'捕获零除', narrativeTitle:'除零保护',
    narrativeDesc:'除法运算中除数为零会报错。用 try-except 处理。', narrativeExplanation:'将可能出错的代码放 try 块。',
    description:'用 try-except 捕获 ZeroDivisionError。', difficulty:2, knowledgeTags:['异常','try'],
    initialCode:'x = 10\ny = 0\n',
    correctCode:'x = 10\ny = 0\ntry:\n    print(x / y)\nexcept ZeroDivisionError:\n    print("不能除零")',
    expectedOutput:'不能除零',
    explanation:'try 块出错跳到 except 块，不会崩溃。',
    hint:'try: ... except 类型:', hintRoleplay:'把可能出错的放 try 里。', hintDirect:'except ZeroDivisionError:' },

  { id:'s6_03', type:'code_fill', chapterId:'ch6_errors', title:'通用捕获', narrativeTitle:'万能防护',
    narrativeDesc:'用通用 except 捕获所有异常。', narrativeExplanation:'不指定类型的 except 捕获所有异常。',
    description:'用 try-except 捕获所有异常。', difficulty:2, knowledgeTags:['异常','except'],
    initialCode:'x = int("abc")\n', correctCode:'try:\n    x = int("abc")\nexcept:\n    print("出错了")',
    expectedOutput:'出错了',
    explanation:'不指定类型的 except 捕获所有异常。',
    hint:'except: 捕获全部。', hintRoleplay:'不指定类型抓住所有错误。', hintDirect:'except:\n    print("出错了")' },

  { id:'s6_04', type:'output_predict', chapterId:'ch6_errors', title:'异常路径', narrativeTitle:'容错路径',
    narrativeDesc:'预测以下代码的输出。', narrativeExplanation:'try 正常执行后不进 except。',
    description:'预测输出。', difficulty:2, knowledgeTags:['异常','路径'],
    initialCode:'try:\n    print("尝试")\nexcept:\n    print("出错")\nprint("结束")',
    options:['"尝试"\n"结束"','"尝试"\n"出错"\n"结束"','"尝试"','"出错"\n"结束"'], correctOption:0,
    expectedOutput:'尝试\n结束',
    explanation:'没出错不进 except。',
    hint:'没错就走 except 外。', hintRoleplay:'没错不进 except。', hintDirect:'尝试\\n结束' },

  { id:'s6_05', type:'code_fix', chapterId:'ch6_errors', title:'修复捕获', narrativeTitle:'捕获纠错',
    narrativeDesc:'修复 try-except 语法错误。', narrativeExplanation:'try 和 except 都需要冒号。',
    description:'修复代码。', difficulty:2, knowledgeTags:['异常','语法'],
    initialCode:'try\n    print(1/0)\nexcept:\nprint("err")',
    correctCode:'try:\n    print(1/0)\nexcept:\n    print("err")',
    expectedOutput:'err',
    explanation:'try 和 except 后面都要加冒号。',
    hint:'加冒号。', hintRoleplay:'try 也要冒号。', hintDirect:'try:\n    print(1/0)\nexcept:' },

  // ========== 第 2 节：异常进阶（5 题）==========
  { id:'s6_06', type:'choice', chapterId:'ch6_errors', title:'异常类型', narrativeTitle:'错误分类',
    narrativeDesc:'不同类型的错误需要不同处理。以下哪个是"索引越界"的异常类型？',
    narrativeExplanation:'IndexError 在访问不存在的列表索引时抛出。',
    description:'以下哪个异常类型表示索引越界？', difficulty:1, knowledgeTags:['异常','类型'],
    options:['IndexError','KeyError','TypeError','ValueError'], correctOption:0,
    explanation:'IndexError 是索引越界异常。',
    hint:'Index = 索引。', hintRoleplay:'IndexError 是索引超出范围。', hintDirect:'IndexError' },

  { id:'s6_07', type:'code_fill', chapterId:'ch6_errors', title:'else 分支', narrativeTitle:'无错分支',
    narrativeDesc:'try-except-else：没有异常时执行 else 块。', narrativeExplanation:'else 只有在 try 没异常时才执行。',
    description:'用 try-except-else 处理除法，无错时打印"计算成功"。', difficulty:2, knowledgeTags:['异常','else'],
    initialCode:'a, b = 10, 2\n',
    correctCode:'a, b = 10, 2\ntry:\n    r = a / b\nexcept ZeroDivisionError:\n    print("不能除零")\nelse:\n    print("计算成功")',
    expectedOutput:'计算成功',
    explanation:'else 块在 try 没有抛出异常时执行。',
    hint:'try 没错就走 else。', hintRoleplay:'try 成功就执行 else。', hintDirect:'else:\n    print("计算成功")' },

  { id:'s6_08', type:'code_fill', chapterId:'ch6_errors', title:'finally', narrativeTitle:'最终执行',
    narrativeDesc:'无论有没有异常都要执行清理工作。用 finally 实现。', narrativeExplanation:'finally 无论 try 是否异常都执行。',
    description:'用 try-finally 确保"清理完成"始终被打印。', difficulty:2, knowledgeTags:['异常','finally'],
    initialCode:'try:\n    print("操作")\n    # 加 finally\n',
    correctCode:'try:\n    print("操作")\nfinally:\n    print("清理完成")',
    expectedOutput:'操作\n清理完成',
    explanation:'finally 无论是否有异常都会执行，适合资源清理。',
    hint:'finally 永远执行。', hintRoleplay:'finally 是无论如何都会执行的。', hintDirect:'finally:\n    print("清理完成")' },

  { id:'s6_09', type:'output_predict', chapterId:'ch6_errors', title:'完整路径', narrativeTitle:'完整流程',
    narrativeDesc:'预测 try-except-else-finally 的输出。', narrativeExplanation:'顺序：try→(出错则 except)→else→finally。',
    description:'预测输出。', difficulty:3, knowledgeTags:['异常','else','finally'],
    initialCode:'try:\n    print(1)\nexcept:\n    print(2)\nelse:\n    print(3)\nfinally:\n    print(4)',
    options:['1\\n3\\n4','1\\n2\\n3\\n4','1\\n2\\n4','1\\n4'], correctOption:0,
    expectedOutput:'1\\n3\\n4',
    explanation:'try 无异常→跳过 except→走 else→最后 finally。',
    hint:'无异常走 else。', hintRoleplay:'没错的路径：try→else→finally。', hintDirect:'1\\n3\\n4' },

  { id:'s6_10', type:'code_fix', chapterId:'ch6_errors', title:'异常修复', narrativeTitle:'异常纠错',
    narrativeDesc:'这段代码的异常处理顺序有误。', narrativeExplanation:'try-except-else-finally 的顺序是固定的。',
    description:'修复异常处理结构的顺序。', difficulty:3, knowledgeTags:['异常','顺序'],
    initialCode:'try:\n    x = 1 / 1\nfinally:\n    print("结束")\nexcept:\n    print("错误")\nelse:\n    print("成功")',
    correctCode:'try:\n    x = 1 / 1\nexcept:\n    print("错误")\nelse:\n    print("成功")\nfinally:\n    print("结束")',
    expectedOutput:'成功\n结束',
    explanation:'正确的顺序：try → except → else → finally。except 在 else 之前。',
    hint:'顺序：try-except-else-finally。', hintRoleplay:'except 要在 else 前面。', hintDirect:'try:\\n    x = 1 / 1\\nexcept:' },

  // ========== 第 3 节：raise 与断言（5 题）==========
  { id:'s6_11', type:'choice', chapterId:'ch6_errors', title:'主动抛出', narrativeTitle:'主动预警',
    narrativeDesc:'有时需要主动抛出异常。哪段代码能手动抛出一个异常？',
    narrativeExplanation:'raise 语句手动抛出异常，通常用于输入验证。',
    description:'以下哪个能手动抛出 ValueError？', difficulty:2, knowledgeTags:['异常','raise'],
    options:['raise ValueError("无效")','throw ValueError("无效")','raise ValueError','error ValueError'],
    correctOption:0,
    explanation:'raise 异常类型("消息") 手动抛出异常。',
    commonErrors:[{ pattern:'用了 throw', message:'Python 用 raise 不是 throw', noxHint:'Python 用 raise 抛出异常。throw 是其他语言的。' }],
    hint:'raise 异常类型(消息)', hintRoleplay:'raise 是"主动报错"。', hintDirect:'raise ValueError("无效")' },

  { id:'s6_12', type:'code_fill', chapterId:'ch6_errors', title:'验证输入', narrativeTitle:'参数验证',
    narrativeDesc:'如果传入负数，主动抛出 ValueError。', narrativeExplanation:'raise 用于参数验证。',
    description:'在 set_age 中验证年龄必须大于 0。', difficulty:2, knowledgeTags:['异常','raise'],
    initialCode:'def set_age(n):\n    if n < 0:\n        # 抛出 ValueError\n    print(f"年龄: {n}")\n\nset_age(5)\ntry:\n    set_age(-1)\nexcept ValueError as e:\n    print(f"捕获异常: {e}")\n',
    correctCode:'def set_age(n):\n    if n < 0:\n        raise ValueError("年龄不能为负")\n    print(f"年龄: {n}")\n\nset_age(5)\ntry:\n    set_age(-1)\nexcept ValueError as e:\n    print(f"捕获异常: {e}")',
    expectedOutput:'年龄: 5',
    validationMode:'contains',
    explanation:'raise 在条件不满足时主动抛出异常，阻止后续执行。外层 try-except 捕获后程序不会崩溃。',
    hint:'if 条件: raise 类型', hintRoleplay:'参数不对就 raise。', hintDirect:'raise ValueError("年龄不能为负")' },

  { id:'s6_13', type:'code_fill', chapterId:'ch6_errors', title:'异常传递', narrativeTitle:'错误上报',
    narrativeDesc:'函数内捕获异常后可以重新抛出给调用者。', narrativeExplanation:'raise 不带参数会重新抛出当前异常。',
    description:'捕获异常后重新抛出。', difficulty:3, knowledgeTags:['异常','raise'],
    initialCode:'def div(a, b):\n    try:\n        return a / b\n    except:\n        # 重新抛出\n\ntry:\n    print(div(10, 0))\nexcept ZeroDivisionError:\n    print("异常传递给了调用者")\n',
    correctCode:'def div(a, b):\n    try:\n        return a / b\n    except:\n        raise\n\ntry:\n    print(div(10, 0))\nexcept ZeroDivisionError:\n    print("异常传递给了调用者")',
    expectedOutput:'异常传递给了调用者', validationMode:'contains',
    explanation:'raise 不带参在当前 except 块中会重新抛出同一异常，由调用者的 try-except 捕获。',
    hint:'在 except 里用 raise。', hintRoleplay:'raise 单独用会重新抛出当前异常。', hintDirect:'except:\\n    raise' },

  { id:'s6_14', type:'output_predict', chapterId:'ch6_errors', title:'异常冒泡', narrativeTitle:'异常传导',
    narrativeDesc:'异常会沿着调用栈向上传导直到被捕获。预测输出：', narrativeExplanation:'函数不处理异常会向上传递到调用者。',
    description:'预测输出。', difficulty:3, knowledgeTags:['异常','冒泡'],
    initialCode:'def inner():\n    raise ValueError("错")\n\ndef outer():\n    inner()\n\ntry:\n    outer()\nexcept ValueError as e:\n    print(e)',
    options:['"错"','ValueError',"外层的 try\\n没捕获到",'程序崩溃'], correctOption:0,
    expectedOutput:'错',
    explanation:'inner 抛出的异常经过 outer 向上传导，在外层的 try-except 中被捕获。',
    hint:'异常向上传到最近的 try。',
    hintRoleplay:'inner 报错→outer 没处理→继续向上→被最外层的 try 抓住。',
    hintDirect:'错' },

  { id:'s6_15', type:'code_fix', chapterId:'ch6_errors', title:'断言修复', narrativeTitle:'断言矫正',
    narrativeDesc:'assert 是 Python 的调试工具。修复以下错误的断言用法。', narrativeExplanation:'assert 条件, "消息"——条件为假时抛出 AssertionError。',
    description:'修复断言语法错误。', difficulty:2, knowledgeTags:['异常','assert'],
    initialCode:'x = 1\nassert x > 0\n    print("正确")',
    correctCode:'x = 1\nassert x > 0, "x 必须大于 0"\nprint("正确")',
    expectedOutput:'正确', validationMode:'contains',
    explanation:'assert 条件, "消息" 当条件为假时抛出 AssertionError。条件为真时程序正常继续。',
    hint:'assert 条件, "消息"', hintRoleplay:'assert 是"断言必须成立"。', hintDirect:'assert x > 0, "x 必须大于 0"' },

  // ========== 第 4 节：pathlib 文件路径（5 题）==========
  { id:'s6_16', type:'choice', chapterId:'ch6_errors', title:'Path 对象', narrativeTitle:'路径术式',
    narrativeDesc:'pathlib 是 Python 的现代路径处理库。哪段代码正确创建了 Path 对象？',
    narrativeExplanation:'Path 对象表示文件系统路径，可以用 / 操作符拼接路径。',
    description:'以下哪个正确创建了 Path 对象？', difficulty:2, knowledgeTags:['pathlib','Path'],
    options:['from pathlib import Path\\nPath("data/file.txt")','import path\\npath.Path("data/file.txt")','from os import path\\npath.join("data","file.txt")','import filepath\\nFilePath("data/file.txt")'],
    correctOption:0,
    explanation:'from pathlib import Path 然后 Path("路径") 创建路径对象。',
    hint:'from pathlib import Path', hintRoleplay:'pathlib 是现代 Python 的路径处理方式。', hintDirect:'from pathlib import Path' },

  { id:'s6_17', type:'code_fill', chapterId:'ch6_errors', title:'路径拼接', narrativeTitle:'路径组合',
    narrativeDesc:'用 / 操作符拼接路径比字符串拼接更安全。', narrativeExplanation:'Path 的 / 操作符自动处理分隔符。',
    description:'用 Path 和 / 拼接路径 data/sub/file.txt。', difficulty:2, knowledgeTags:['pathlib','Path'],
    initialCode:'from pathlib import Path\nbase = Path("data")\n# 拼接 sub/file.txt\n',
    correctCode:'from pathlib import Path\nbase = Path("data")\np = base / "sub" / "file.txt"\nprint(p)',
    expectedOutput:'data/sub/file.txt',
    explanation:'Path / "子路径" 自动处理路径分隔符（本环境为 Linux 风格，输出正斜杠 /）。',
    hint:'Path / "子路径"', hintRoleplay:'/ 操作符能自动处理 Windows 和 Linux 的路径差异。', hintDirect:'base / "sub" / "file.txt"' },

  { id:'s6_18', type:'code_fill', chapterId:'ch6_errors', title:'路径属性', narrativeTitle:'路径信息',
    narrativeDesc:'Path 对象可以获取文件名、扩展名等信息。', narrativeExplanation:'.name 获取文件名，.suffix 获取扩展名。',
    description:'打印 Path("data/report.txt") 的文件名和扩展名。', difficulty:2, knowledgeTags:['pathlib','属性'],
    initialCode:'from pathlib import Path\np = Path("data/report.txt")\n# 打印文件名和扩展名\n',
    correctCode:'from pathlib import Path\np = Path("data/report.txt")\nprint(p.name, p.suffix)',
    expectedOutput:'report.txt .txt',
    explanation:'.name 是文件名，.suffix 是扩展名（含点号）。',
    hint:'p.name 和 p.suffix', hintRoleplay:'.name 是文件名，.suffix 是扩展名。', hintDirect:'print(p.name, p.suffix)' },

  { id:'s6_19', type:'code_fix', chapterId:'ch6_errors', title:'路径修复', narrativeTitle:'路径纠错',
    narrativeDesc:'这段代码试图用字符串拼接路径但问题多多。改用 pathlib。', narrativeExplanation:'用 pathlib 替代字符串拼接，避免跨平台问题。',
    description:'改用 Path 修复路径处理。', difficulty:2, knowledgeTags:['pathlib','Path'],
    initialCode:'folder = "data"\nfile = "report.txt"\npath = folder + "\\\\" + file  # Windows 写法，在 Linux/Mac 上分隔符错误\nprint(path)',
    correctCode:'from pathlib import Path\nfolder = "data"\nfile = "report.txt"\npath = Path(folder) / file\nprint(path)',
    expectedOutput:'data/report.txt',
    explanation:'Path(folder) / file 替代 folder + "/" + file，更安全、更跨平台。',
    hint:'用 Path 和 /。', hintRoleplay:'Path(folder) / file 比字符串拼接优雅多了。', hintDirect:'Path(folder) / file' },

  { id:'s6_20', type:'free_coding', chapterId:'ch6_errors', title:'安全读取', narrativeTitle:'安全读取文件',
    narrativeDesc:'编写函数 safe_read(path)，用 try-except 安全读取文件内容。如果文件不存在返回空字符串，其他错误返回 None。',
    narrativeExplanation:'综合运用异常处理 pathlib 读取文件、处理错误。',
    description:'实现 safe_read，成功返回内容，FileNotFoundError 返回""，其他异常返回 None。',
    difficulty:4, knowledgeTags:['异常','pathlib','综合'],
    initialCode:'def safe_read(path):\n    # 用 try-except 安全读取文件\n    pass\n',
    correctCode:'def safe_read(path):\n    try:\n        with open(path) as f:\n            return f.read()\n    except FileNotFoundError:\n        return ""\n    except:\n        return None\n',
    testCases:[
      { input:'repr(safe_read("nonexistent.txt"))', expected:"''" },
    ],
    explanation:'try 块读取文件，FileNotFoundError 返回空串，其他异常返回 None。',
    hint:'try: open → except FileNotFoundError → except 通用', hintRoleplay:'用 try 包裹文件操作，分类处理不同类型的错误。', hintDirect:'try:\\n    with open(path) as f:\\n        return f.read()' },
]
